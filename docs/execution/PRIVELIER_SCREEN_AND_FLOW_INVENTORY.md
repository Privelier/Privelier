# Privelier screen and flow inventory

Status: in progress — routing and every screen/control data path are traced; cross-cutting state and deep-link verification remain open under `P00-004`.
Last updated: 2026-09-08
Evidence source: `App.tsx`, the three navigators, the two tab shells, and the listed screen modules. This document makes no claim that a named control is functional until its handler and data path have been traced.

## Root switch

| ID | Phase / surface | Entry | Exit | Role boundary | Verified selector / state |
| --- | --- | --- | --- | --- | --- |
| ROOT-01 | Native restoring splash | Cold launch while fonts/session restore. | Auth or provisioning/authenticated root after state resolves. | No navigator mounts. | `App.tsx` returns `null` while restoring. |
| ROOT-02 | Unauthenticated shell | No session/profile. | Auth route or authenticated root after session/provisioning. | Shared only here. | `AuthNavigator`. |
| ROOT-03 | Provisioning shell | Session without a completed public profile. | Setup submit, retry, or sign out. | No role navigator until server role resolves. | `FinishSetupScreen` / `ProvisioningScreen`. |
| ROOT-04 | Customer root | Authenticated `public.users.role = customer`. | Sign out. | Customer navigator only. | `CustomerNavigator`. |
| ROOT-05 | Barber root | Authenticated `public.users.role = barber`. | Sign out. | Barber navigator only. | `BarberNavigator`. |
| ROOT-06 | Admin unsupported state | Authenticated `public.users.role = admin`. | Sign out. | Admin app deliberately absent in MVP. | `admin-screen`, `admin-sign-out`. |

## Auth and provisioning surfaces

| ID | Route / screen | Entry and exit | Known controls / selectors | Data or action status |
| --- | --- | --- | --- | --- |
| AUTH-01 | `RoleSelect` | Root unauthenticated entry → `AuthEntry(role)`. | `role-select-customer`, `role-select-barber`. | Functional navigation only; `onSelectRole` passes the selected role as a copy/signup input, never routing authority. |
| AUTH-02 | `AuthEntry` | From role select; back returns to it; enters login or signup. | `auth-entry-screen`, `auth-entry-back`, `auth-entry-login`, `auth-entry-signup`. | Functional local-stack navigation; no data read/write. |
| AUTH-03 | `Login` | From auth entry or signup; back and signup link available. | `auth-login-email`, `auth-login-password`, `auth-login-password-toggle`, `auth-login-submit`, `auth-login-go-signup`, `auth-login-error`, dynamic `*-error`. | Functional: local validation → `supabase.auth.signInWithPassword`; a successful auth event hands control to the root provisioning switch; unconfirmed email routes to AUTH-05; typed errors render inline. |
| AUTH-04 | `Signup` | From auth entry or login; reaches await-confirmation or login. | `auth-signup-name`, `auth-signup-email`, `auth-signup-password`, `auth-signup-city`, optional `auth-signup-country`, `auth-signup-phone`, barber-only `auth-signup-bio`, submit/login/error and dynamic `*-error` selectors. | Functional: local validation → `supabase.auth.signUp` with role/profile metadata as a prefill hint; confirmation route on success; duplicate-email path exposes a login link; no profile-table write occurs here. |
| AUTH-05 | `AwaitEmailConfirmation` | From signup; back to login or resend. | `auth-confirm-screen`, `auth-confirm-back-to-login`, `auth-confirm-resend`, `auth-confirm-success`, `auth-confirm-error`. | Functional: resend → `supabase.auth.resend({ type: 'signup' })`; button is busy/disabled during request; success/error feedback is inline. |
| AUTH-06 | `FinishSetup` | Root provisioning setup-form view; continue or sign out. | `setup-role-customer`, `setup-role-barber`, `setup-role-error`, `setup-name`, `setup-city`, optional country/phone and barber-only bio, `setup-submit`, `setup-sign-out`, `setup-error`. | Functional: validates required role/name/city → shell `ensureProfileFromForm`; writes only allowed profile fields, then root switch changes surface for ready/signed-out; error remains inline. |
| AUTH-07 | `Provisioning` | Root provisioning loading/failure view; retry or sign out. | `provisioning-screen`, `provisioning-loading`, `provisioning-failure`, `provisioning-retry`, `provisioning-sign-out`. | Functional: loading is read-only; failure retry invokes shell `ensureProfile`; sign-out clears server session or local fallback. |

### Auth control and state trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Role selection and auth entry | `RoleSelectScreen` calls `AuthNavigator`'s `onSelectRole`, which navigates to `AuthEntry`; entry buttons use stack navigation. | No session or database action. Selecting a role does not authorize the user or select the post-login app. | Indirectly covered by both auth Maestro flows; no dedicated screen-unit test found. |
| Login | `LoginScreen.onSubmit` validates input and calls `authService.signIn` → `supabase.auth.signInWithPassword`. | Required-field errors; submit busy state; inline mapped auth failure; `email_not_confirmed` routes to confirmation; `signed_in` waits for the session-driven root switch. The login route's role affects copy only. | `authService.test.ts` covers success, unconfirmed-email, and invalid-credential mappings; `login-provisioning-role-landing.yaml` covers the customer happy path but is unrun (no Maestro/device). |
| Signup | `SignupScreen.onSubmit` calls either `signUpCustomer` or `signUpBarber`, both using `supabase.auth.signUp`; the service passes `emailRedirectTo` and profile metadata. | Required name/email/password/city; optional country/phone/bio; barber-only verification copy; submit busy state; inline errors; duplicate-account/login alternative. Profile creation is deliberately deferred to provisioning, so signup cannot set server-owned role fields in `public.users`. | `authService.test.ts` covers customer/barber payloads, duplicate-email cases and typed errors; `signup-await-confirmation-resend.yaml` covers the customer route, unrun. |
| Confirmation wait | `AwaitEmailConfirmationScreen.onResend` calls `resendConfirmation` → `supabase.auth.resend`. | Busy/disabled actions and success/error notice. Back returns to the role's Login route. | Service success/rate-limit mapping is unit covered; the unrun signup Maestro flow checks the success notice. |
| Deferred profile setup | `FinishSetupScreen` invokes root-shell `submitSetupForm` → `ensureProfileFromForm`; service reads `users` for the session UID, inserts the allowed profile fields only if missing, and ensures a barber profile row for barber role. | Required role/name/city validation; optional fields trimmed; live root unmount on ready or signed-out; unexpected repeated setup request and errors stay inline. Server-side role is the source of routing truth. | `authService.test.ts` and `useAuthShell.test.ts` cover provisioning/result branches; no dedicated rendered-screen test found. |
| Provisioning failure/sign-out | Root shell invokes `ensureProfile` on a session; retry invokes it again. `signOut` calls Supabase sign-out and attempts a local sign-out if the server call fails. | Loading/failure split; retry remains available; sign-out must not throw; no role navigator mounts before a ready server profile. | `useAuthShell.test.ts` covers retry, auth-event stability and sign-out delegation; `authService.test.ts` covers local-fallback behavior. The live first-device path remains blocked by absent device/Maestro. |
| Admin unsupported | `AdminNotSupportedScreen` only exposes root-shell sign-out. | Intentional MVP limitation, not a missing admin app: founders use Supabase dashboard for Phase 1. | No dedicated rendered-screen test found. |

## Customer navigation inventory

Customer currently exposes five peer tabs: Discover, Explore, Bookings, Inbox, and Account. This is evidence for P04’s approved four-tab consolidation target; no consolidation has been performed.

| ID | Route / screen | Entry and exit | Known controls / selectors | Data or action status |
| --- | --- | --- | --- | --- |
| CUS-01 | `CustomerTabs / Discover` | Customer root; barber card enters profile. | `customer-tab-discover`, `customer-home-screen`, search, chip, loading/error/empty and dynamic `customer-home-barber-{id}` selectors. | Functional read-only discovery: own profile city → approved-city directory → batched service enrichment. Search/chips are local filters; card routes to `BarberProfile`. |
| CUS-02 | `CustomerTabs / Explore` | Customer root; map/list selectors and barber cards. | `customer-tab-explore`, `customer-explore-screen`, `customer-explore-toggle-{list\|map}`, `customer-explore-chip-{key}`, loading/error/empty/map and dynamic barber selectors. | Functional directory/read-only filters and profile navigation. Native-map-unavailable state is intentionally unavailable and says so plainly; no fake map/pin is rendered. |
| CUS-03 | `CustomerTabs / Bookings` | Customer root; row actions can cancel/review. | `customer-tab-bookings`, `customer-bookings-{screen\|tab-*\|loading\|error\|empty\|row-*}`, `booking-cancel-{id}`, `booking-review-{id}`. | Functional own-booking read/realtime merge, guarded optimistic cancellation, and completed-booking review entry. |
| CUS-04 | `CustomerTabs / Inbox` | Customer root; room row enters conversation. | `customer-tab-inbox`, `customer-inbox-{screen\|loading\|error\|empty\|row-{id}\|unread-{id}}`. | Functional participant-scoped room/message list, observed unread state, and conversation navigation. |
| CUS-05 | `CustomerTabs / Account` | Customer root; sections and sign-out. | `customer-tab-account`, `customer-account-screen`, dynamic `customer-account-row-{section}`, `customer-account-logout`. | Profile read is functional; sign-out is functional. The five setting rows deliberately lead to honest, not-yet-implemented sections. |
| CUS-06 | `BarberProfile` | From discovery/explore card; back to source; selected service opens booking flow. | `barber-profile-{screen\|back\|loading\|error\|not-found\|rating\|tab-{key}\|service-{id}\|book-{id}\|portfolio-*\|reviews-*}`. | Functional approved public profile with independent service/portfolio/review reads; booking action routes with existing service data. |
| CUS-07 | `AccountSection` | From account row; back to account. | `customer-account-section-screen`, `customer-account-section-back`. | Intentional MVP placeholder surface with scoped explanatory copy; Favorites, notification settings, privacy, preferences, and help operations are not yet implemented. |
| CUS-08 | `BookingDateTime` | From selected service; continue to location. | `customer-booking-datetime-{screen\|back\|loading\|error\|empty\|no-times\|continue\|summary}`, dynamic `day-{index}` and `slot-{HH:MM:SS}`. | Functional availability/busy-slot read, local date/slot selection, then route to location. Re-fetches when focused after an insert conflict. |
| CUS-09 | `BookingLocation` | From date/time; continue to confirm. | `customer-booking-location-{screen\|back\|input\|continue}`. | Functional editable free-text location; customer city is a best-effort prefill only; local nonblank guard routes to confirmation. |
| CUS-10 | `BookingConfirm` | From location; submit or select another time. | `customer-booking-confirm-{screen\|back\|submit\|success\|error\|pick-another-time}`. | Functional guarded booking insert; server snapshots price/status, double-booking conflict is explicit, and success resets to Bookings. |
| CUS-11 | `Conversation` | From Inbox; back to Inbox. | `customer-conversation-{screen\|back\|loading\|error\|empty\|input\|send\|counter\|typing\|read-marker}`, dynamic message/pending/failed selectors. | Functional focus-gated message realtime, non-fabricated optimistic send/retry, read receipt, and typing broadcast. |
| CUS-12 | `ReviewSubmit` | From completed booking; brief success then back to bookings. | `customer-review-submit-{screen\|back\|stars\|stars-star-{1..5}\|comment\|submit\|error\|success}`. | Functional 1–5 star review write; server verifies participant/completion, owns aggregation, and rejects duplicate review. |

### Customer discovery control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Discover | Mount load calls `fetchOwnProfile` → `public.users` for the current session, then `listBarbersByCity` → approved-only `barber_directory`, followed by one batched `services` read. | Profile/directory errors render `customer-home-error`; no result and filtered-out result use distinct truthful empty copy. Service enrichment is decorative: its failure removes price/filter details rather than hiding discovered barbers. Search and service chips only update local state. | `discoveryData.test.ts` covers the data module. `customer-discovery-view-barber-profile.yaml` covers login → approved same-city barber → profile but is unrun pending Maestro/device/test accounts. |
| Discover barber cards | `BarberCard.onPress` routes `BarberProfile` with the displayed directory id; featured and nearby rail cards share that handler. | Directory view/RLS remains the eligibility boundary; the screen does not manufacture results or bypass verification. Dynamic selector is `customer-home-barber-{id}`. | Covered as part of the unrun discovery Maestro flow; profile control trace remains open under CUS-06. |
| Explore | Mount load uses the same own-profile/city/directory chain, plus one batched `services` read and one batched `availability` read. List/map toggle and filter chips only update local state; barber card routes to `BarberProfile`. | Profile/directory failure renders `customer-explore-error`; empty city/filter result has truthful copy. A failed services or availability enrichment hides its dependent filter rather than asserting no barbers match. `all` and `verified` do not need enrichment. | `exploreData.test.ts` covers the data/filter module. `customer-explore-filters-list.yaml` covers load, always-available chips, toggle and verified-filter rerender; it is unrun pending Maestro/device/test account. |
| Explore map | `isMapNativeAvailable()` guards a lazy `require()` of the native map surface; pins derive only from the filtered list. | If the native module is absent, `customer-explore-map-soon` explains that map support requires the next app update. With native support but no located barbers, `customer-explore-map-empty` renders. Both are intentional, honest states; neither uses a default or fake pin. | The unrun Explore Maestro flow asserts map-area stability only, deliberately accepting real-map, empty-map, or map-soon outcomes. |

### Customer booking control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Date and time | On focus, load calls `listBarberAvailability` for the selected barber and `get_barber_busy_slots` once per date in the 14-day lookahead; `deriveAvailableSlots` subtracts occupied time from recurring/specific availability using the selected service duration. Day/slot controls update local selection and Continue routes to location. | Loading, availability error, all-dates-empty, selected-date-no-times, disabled Continue, and selected summary are distinct. A busy-slot read failure currently degrades that date to an empty busy list; the authoritative unique booking constraint still catches a racing/occupied slot at confirmation. Returning from a conflict re-focuses and refreshes slots. | `slots.test.ts` and `bookingTime.test.ts` cover slot derivation/time semantics; `customer-booking-create.yaml` covers a happy-path traversal but is unrun pending Maestro/device/test data. |
| Location | A best-effort `fetchOwnProfile` reads the current customer's city and fills only an empty field; Continue passes trimmed free text to confirmation. | No geocoding/location permission/API is requested. City alone is not treated as a sufficient address; empty/whitespace input keeps Continue disabled. | Traversed in the unrun booking Maestro flow; no dedicated rendered-screen test found. |
| Confirmation and write | Confirm calls `insertBooking`, which reads the current auth UID, inserts `customer_id`, barber/service/date/time/location into `bookings`, and returns typed `ok`, `conflict`, or error. | The client never sends `price` or `status`: a database trigger snapshots price and the DB defaults/enforces pending. The active-slot unique index is the final double-booking guard. Conflict is never auto-retried; it offers a return to Date/Time. Success is shown briefly, then navigation resets to CustomerTabs → Bookings so the flow cannot be resubmitted with Back. | `bookingCreateData.test.ts` covers insert result/error mapping; the unrun booking Maestro flow checks submit → success → Bookings visibility. |

### Customer bookings and review control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Bookings list and realtime | Focus load calls `fetchOwnBookingsView` → participant-scoped `bookings`, with best-effort directory/service enrichment, then obtains the current session UID to filter `useBookingsRealtime` on `customer_id`. Reconnect recovery reloads a baseline and every snapshot/event merges idempotently by booking id. | Loading/error are blocking only before any data arrives; later failed refresh preserves visible rows. Upcoming/Past is a local time/status projection. Status events are live only while the tab is focused. | `bookingsData.test.ts`, `bookingRealtime.test.ts`, and `useBookingsRealtime.test.ts` cover data/merge/subscription behavior. The booking Maestro flow verifies the created booking appears, but remains unrun. |
| Cancellation | An actionable pending/accepted row opens a destructive native confirmation. Confirming applies an optimistic cancelled row, guards double taps, then calls `cancelBookingAsCustomer` → `bookings.update({ status: 'cancelled' })`. | DB trigger/RLS define the actor and allowed state transitions. On mutation failure, the prior row is restored and a row-scoped error is shown; success reapplies the authoritative returned row. Terminal rejected/completed/cancelled rows render no cancel action. | `bookingsData.test.ts` covers typed cancellation outcomes; full two-role/realtime device path remains unrun. |
| Review entry and submission | Completed, unreviewed rows navigate to ReviewSubmit. The screen requires a local 1–5 rating, limits optional comment UI text to 500 characters, and calls `submitReview` → `reviews.insert` with the session customer id. | The completed-booking/customer/barber relationship is enforced by hardened RLS, not trusted route parameters. The database owns barber rating aggregation. A booking-id unique conflict becomes a calm already-reviewed terminal state; other errors are inline. Success returns to Bookings, which refreshes reviewed ids. | `reviewsData.test.ts`, rating component tests, and the ReviewSubmit rendered-screen test cover service/UI branches. `customer-review-submit.yaml` is authored but unrun pending Maestro/device/test data. |

### Customer inbox, chat, and account control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Inbox | Focus load calls `fetchOwnInboxView`: participant-scoped `chat_rooms`, capped latest-message scan, and best-effort booking/barber/service enrichment. Thread tap routes to Conversation with the existing room and loaded header context. | Loading/error/empty states are explicit. Latest-message preview falls back to real booking service context; no fake online/presence indicator exists. `UnreadProvider` marks real unread rooms separately. | `inboxData.test.ts`, shared unread tests, and customer-chat Maestro flows cover the data/realtime primitives; the Maestro flows are unrun pending device/CLI/test users. |
| Conversation | Focus opens a message channel before the baseline `fetchConversation` read; snapshot and realtime events idempotently merge by message id; reconnect re-fetches. Send uses session-derived sender id through `sendMessage`, with `useSendQueue` rendering local pending/failed states only until an authoritative row arrives. | Blank sends are disabled; max length matches the server limit; same-tick double send is synchronously guarded; failed messages persist only in current screen state and can retry. Read receipt reads the counterpart's observed `chat_read_state`; typing is a participant-only ephemeral broadcast. No presence, fabricated timestamps, or persisted fake rows. | `conversationData.test.ts`, `useSendQueue`, unread, receipt and typing hook tests cover core branches. `customer-chat-send-message.yaml` and `customer-chat-read-receipt.yaml` are authored but unrun. |
| Account | Mount fetches own profile for display. Each setting row uses local-stack navigation to AccountSection; sign-out delegates to the root `signOutNow` path. | Profile read failure degrades to neutral fallback display. Wallet/payments and gift cards are intentionally absent (out of MVP scope). | The auth sign-out path is unit-covered and reached in the unrun login Maestro flow; no dedicated Account rendered-screen test found. |
| Account sections | Each section uses a static, honest explanatory body and only a Back action. | Favorites, notification preferences, privacy controls, and preferences are intentionally unavailable in this MVP. Help presents the project contact address, but does not send a message or create a support case. | No dedicated tests found; the static/intentional-unavailable classification is source-verified. |

### Customer barber-profile control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Barber profile | Mount read calls `getBarberProfile` against approved-only `barber_directory`, then independently loads that barber's services, portfolio rows, and reviews/first-name projection in parallel. | Top-level load, unavailable/not-found, and error states are distinct. Secondary fetch failures are tab-local, so a readable public profile remains usable. Profile data supplies a fallback initial/location/rating only when the respective real field is absent. | `discoveryData.test.ts` covers profile/service/portfolio reads; the unrun discovery Maestro flow covers profile load, service state, and Back. |
| Tab strip and public content | Services, Portfolio, and Reviews buttons update local tab state. Portfolio tiles derive URLs through the shared portfolio component; reviews use the server-owned aggregate plus submitted review rows. | Empty services/portfolio/reviews each render honest copy. Reviewer identity is best-effort first-name projection; an unavailable name does not expose full account data or invalidate the review. | Data/review modules are unit covered; no dedicated tabbed-profile rendered-screen test found. |
| Book action | Each real service button routes to BookingDateTime with barber id/name and the fetched service row. | The route data is only a selection/summary convenience; availability, server price snapshot, RLS, and unique-slot enforcement remain in the subsequent booking flow/database. | Traversed in the unrun booking Maestro flow. |

## Barber navigation inventory

Barber currently exposes five peer tabs: Studio, Requests, Portfolio, Chats, and Verify. This is evidence for P04’s approved four-tab consolidation target; no consolidation has been performed.

| ID | Route / screen | Entry and exit | Known controls / selectors | Data or action status |
| --- | --- | --- | --- | --- |
| BAR-01 | `BarberTabs / Studio` | Barber root; links to requests, services, availability, bio, location, portfolio, verification; sign out. | `barber-tab-studio`, `barber-dashboard-{screen\|logout\|loading\|error\|verification\|overview\|readiness\|readiness-{key}\|services\|availability\|bio\|location}`. | Functional focus-refreshed, read-only dashboard with management/deep-link actions and manual-verification status. |
| BAR-02 | `BarberTabs / Requests` | Barber root; booking actions accept, reject, cancel, or complete. | `barber-tab-requests`, `barber-requests-{screen\|loading\|error\|empty\|row-*}`, `request-{accept\|reject\|cancel\|complete}-{id}`. | Functional participant-scoped booking list, focus-gated realtime, actor-enforced optimistic transitions with rollback. |
| BAR-03 | `BarberTabs / Portfolio` | Barber root; pick/upload or delete own public portfolio image. | `barber-tab-portfolio`, `barber-portfolio-{screen\|counter\|loading\|error\|add\|uploading}`, dynamic image/delete selectors. | Functional owner-folder upload then row insert, hard DB cap of six, and confirmation-gated optimistic delete with recovery. |
| BAR-04 | `BarberTabs / Chats` | Barber root; room row enters conversation. | `barber-tab-chats`, `barber-chats-{screen\|loading\|error\|empty\|row-*\|unread-*}`. | Functional participant-scoped thread list; focus refresh plus live unread-state presentation. |
| BAR-05 | `BarberTabs / Verify` | Barber root; observes manual status and uploads/replaces ID or licence. | `barber-tab-verify`, `barber-verify-{screen\|loading\|error\|status\|doc-id\|doc-license}`, dynamic `*-uploading`. | Functional private-document upload then own request-row update; status stays founder/admin-owned and manual-only. |
| BAR-06 | `Services` | From Studio; back to Studio. | `barber-services-{screen\|back\|name\|price\|duration\|submit\|form-error\|cancel-edit}`, dynamic row/edit/delete selectors. | Functional own-service CRUD with local numeric validation and destructive delete confirmation. |
| BAR-07 | `Availability` | From Studio; back to Studio. | `barber-availability-{screen\|back\|mode-{day\|date}\|day-{0..6}\|date\|start-time\|end-time\|submit\|form-error\|cancel-edit}`, dynamic row/edit/delete selectors. | Functional own-window CRUD with mutually-exclusive recurring/specific-date form and destructive delete confirmation. |
| BAR-08 | `BioEdit` | From Studio; back/discard or save. | `barber-bio-{screen\|back\|loading\|load-error\|error\|input\|counter\|clear-hint\|save}`. | Functional own-bio fetch/update; authoritatively refreshes, avoids no-op writes, and warns before losing unsaved prose. |
| BAR-09 | `LocationEdit` | From Studio; back or save. | `barber-location-{screen\|back\|loading\|load-error\|consent\|error\|input\|searching\|search-error\|no-results\|candidates\|candidate-*\|pick-hint\|save}`. | Functional own-location fetch/upsert with consent copy and candidate-only geocoded coordinates; exit currently discards an unsaved local draft without a confirmation. |
| BAR-10 | `Conversation` | From Chats; back to Chats. | `barber-conversation-{screen\|back\|loading\|error\|empty\|message-*\|sending-*\|failed-*\|failure-reason-*\|read-marker\|typing\|counter\|input\|send}`. | Functional participant-scoped conversation with focus-gated realtime, optimistic send/retry, observed receipt/typing state, and mapped error states. |

### Barber Studio and requests control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Studio dashboard | Focus load first reads own profile for identity, then `fetchDashboardView` composes owner-scoped requests, services, availability, portfolio, barber profile, and location reads. The overview routes to Requests; management/readiness controls route to the respective barber-only surface; sign-out uses the shared root path. | Initial identity failure produces the only whole-dashboard error. Once loaded, later focus refresh failures preserve the last good dashboard. Per-field dashboard data degrades to neutral summaries rather than inventing completion. The five readiness items are services, availability, portfolio, bio, and manual verification; only unfinished items are actionable. | `dashboardData.test.ts` covers pure derivations. `barber-dashboard-overview-readiness.yaml` is unrun and currently asserts only four readiness rows, omitting the current bio item; this is a P00 coverage finding. |
| Verification status on Studio | Dashboard displays the `barber_profile` verification status returned by the owner-scoped profile read. | Approved says the barber appears in search; pending is a calm manual-review status; rejected directs to Verify. The barber cannot set this status from Studio. | Profile/dashboard data tests cover derivation. Real founder review/device evidence remains external. |
| Requests list and realtime | Focus opens `useBookingsRealtime` filtered by `barber_id` before `fetchOwnRequestsView`; snapshots and events merge/re-sort idempotently; reconnect reloads. Services and customer-counterpart projection are best-effort enrichment. | Loading/error are blocking only before any rows arrive. Later failed refresh preserves rows. The counterparty RPC limits identity exposure to the other participant; fallbacks retain the booking/service context. | `requestsData.test.ts` plus shared booking realtime tests cover data/reconciliation. The accept/reject Maestro flow is authored but unrun. |
| Booking transitions | Pending rows expose one-tap Accept and confirmed destructive Reject; accepted rows expose one-tap Mark complete and confirmed destructive Cancel. Each action optimistically updates, guards double taps, calls a typed `bookings.update`, then reapplies authoritative row or rolls back with row error. | The client does not decide authorization: RLS plus the actor-aware trigger enforce only-barber accept/reject/complete and participant accepted-cancel. Terminal states have no action. | `requestsData.test.ts` covers mutation outcomes. `barber-requests-accept-reject.yaml` exercises optimistic accept/reject and terminal actions, but remains unrun; customer-side instant update still needs the two-device realtime gate. |

### Barber service and availability control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Services | Mount reads the current session for the barber id, then `listOwnServices`. Add/Edit validates name, nonnegative numeric price, and duration before `createService`/`updateService`; success resets form and refreshes. | Own-row RLS and barber role are authorization; the screen maps errors rather than trusting client checks. Duplicate name is an informational warning only (the schema deliberately has no name uniqueness rule). Deleted/not-found rows are removed locally; failed deletion uses a native error alert. | `servicesData.test.ts` covers data outcomes. `barber-services-add-edit-delete.yaml` covers create/edit/destructive delete but is unrun pending Maestro/device/test barber. |
| Availability | Mount resolves the own barber id and `listOwnAvailability`. Add/Edit uses either a weekday or a specific date, validates date/time syntax and start-before-end, then calls create/update; success resets and refreshes. | The segmented mode clears the other date representation; DB CHECKs remain the backstop for exactly-one day/date and time order. Existing-window overlap prevention is deliberately not implemented in this slice. Delete is confirmation-gated; failed delete shows a native error alert. | `availabilityData.test.ts` covers data outcomes. `barber-availability-add-edit-delete.yaml` covers create/edit/destructive delete but is unrun pending Maestro/device/test barber. |

### Barber portfolio and verification control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Portfolio | Own profile/id load precedes `listOwnPortfolio`. Add requests photo-library permission, picks an image, uploads bytes to the public `portfolio` bucket under a unique barber-folder path, then inserts its path in `portfolio`; only the returned row appears. | A synchronous busy guard prevents double upload; picker cancel/permission denial adds no row. UI counter/add tile mirror the hard per-barber six-row trigger, which remains authoritative. Delete is confirmation-gated and optimistically removes only after the user confirms; a DB failure restores the tile. DB row removal precedes best-effort storage deletion, so a storage failure can leave an accepted public orphan object; this existing tradeoff remains tracked in the backlog. | `portfolioData.test.ts` covers upload/limit/delete results. `barber-portfolio-upload-delete.yaml` is authored but unrun pending Maestro/device/permission flow. |
| Manual verification | Own profile/id load is followed by owner-scoped barber-profile status and verification-request reads. Each document action requests photo permission, uploads the image to the private `verification-docs` bucket at a unique owner-folder path, then writes only the corresponding path to the barber's own request row and reloads on success. | Upload order prevents document-row references to failed objects. Neither status, reviewer attribution, nor any barber-profile verification field is sent by the client; the database re-queues changed documents as pending for manual founder review. No OCR, selfie, biometric processing, public URL, or signed URL appears in the flow. | `verificationData.test.ts` covers upload/submit cases. `barber-verify-upload-documents.yaml` is authored but unrun pending Maestro/device/permission flow and founder review evidence. |

### Barber chat control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Chats list and unread presentation | Every tab focus calls `fetchOwnChatsView` → RLS-scoped `chat_rooms`, bounded newest-message scan, participant-scoped bookings, and best-effort service enrichment. The barber unread provider maintains the room-id set independently; unread rows use only a brass dot and stronger preview weight. A row routes to the room with its booking/service context; the conversation independently obtains the customer's permitted counterpart name through `get_booking_counterparts`. | Loading, mapped error, and honest no-conversations states are distinct. The list deliberately uses a neutral avatar/service title because a customer's identity is not readable list-side under `users` RLS. No room is created by this screen; chat rooms arrive only through the booking/chat flow. | `conversationData.test.ts` covers barber message operations; shared unread/message/realtime helpers have unit coverage. Customer-side `customer-chat-send-message.yaml` and `customer-chat-read-receipt.yaml` exist but are unrun; no barber-specific chat Maestro flow was located, so both-direction device proof remains under RISK-017. |
| Conversation, realtime, and send recovery | Focus enters the room in `UnreadContext`, fetches RLS-scoped `messages`, opens the room-specific realtime channel before the baseline refetch, and reloads after a reconnect gap. Realtime changes are idempotently merged/sorted. `sendMessage` obtains the local session id, inserts only `{chat_id, sender_id, message}`, then merges the returned authoritative row; the local send queue gives immediate pending bubbles and retryable mapped failures. | Read markers and typing are observed counterpart state only. Mark-as-read is triggered by opening this conversation, not by merely loading the list. Typing traffic is debounced inside its hook, draft input is capped by the shared message limit, and synchronous draft clearing prevents a double send. Initial loading/error are blocking; later reload failure leaves already-read messages usable. Receipt/typing/realtime channels are focused-only and cleaned up on blur. | Barber `conversationData.test.ts` and shared `messageRealtime`, `readReceipts`, `messageLimits`, and `useUnreadThreads` tests cover data/merge primitives. No current barber conversation Maestro flow was found; participant-to-participant realtime, retry, receipt, and typing still require the existing two-device/device-tooling gate. |

### Barber bio and location control trace

| Surface | Handler/data path | Important states and guardrails | Coverage located |
| --- | --- | --- | --- |
| Bio editor | Mount obtains the current session id, then reads the authoritative `barber_profile` row with `fetchOwnBarberProfile`; it does not trust a dashboard seed. Save calls `updateOwnBio`, which trims text and stores an empty bio as `NULL`, then returns to Studio for its focus refresh. | The field is capped at the shared 500-character database limit. Save is disabled for a clean editor, concurrent submission, or missing own id; clearing is an explicit valid remove state. A `beforeRemove` listener protects both header and hardware back from discarding dirty prose; mapped profile/load/form errors are distinct. | `BioEditScreen.test.tsx` exercises editor state/guard behavior; profile-data coverage supplies update outcomes. `barber-bio-edit.yaml` is authored but unrun pending Maestro/device/test barber. |
| Location editor | Mount obtains the current session id and reads its own `barber_location`. Typing debounces Mapbox forward geocoding; every new query cancels the preceding timer and invalidates an out-of-date selection. Only a picked candidate may save nonempty address/coordinates through `updateOwnLocation`; clear writes all three values as null. The DB trigger, not the client, owns display/offset coordinates. | Consent copy says customers see only a shifted approximate area and never the stored street address. Candidate-only save, coordinate-pair/range validation, and a 300-character cap prevent free-text or stale coordinates. Search/loading/no-result/network/form states are explicit. Unlike Bio, `BackButton` has no dirty-draft confirmation: navigation away loses an unsaved local address without a server write; this consistency gap is tracked before any behavior change. | `locationData.test.ts`, `geocoding.test.ts`, and `LocationEditScreen.test.tsx` cover typed operations, provider mapping, debounce, and rendered states. `barber-location-edit.yaml` is authored but unrun pending Maestro/device/network/test barber. |

## Cross-cutting state, deep-link, and selector audit

| Area | Evidence-backed current behavior | Validation and boundary |
| --- | --- | --- |
| Root state | `useAuthShell` is the sole root state machine: restoring keeps the native splash visible without mounting a navigator; no session mounts Auth; a session without a usable profile mounts Provisioning/setup; an authenticated `public.users.role` mounts exactly the Customer, Barber, or admin-not-supported root. A sign-out clears cached session/profile/view state; token refresh does not remount a role navigator while its profile is cached. | `useAuthShell.test.ts` covers the derived phase transitions with mocked auth/profile work. The single root switch is session/profile-derived, never route metadata or a selected login role. |
| Deep links | `app.json` registers the `privelier` scheme. The only application-handled deep-link entry is `privelier://auth-callback`: cold-start and running-app URLs are parsed by `applyAuthCallbackUrl`, which accepts a token pair into `supabase.auth.setSession`; stale confirmation links and malformed/unrelated URLs do not create a session. `NavigationContainer` has no route-linking configuration, so customer/barber screens are not externally deep-linkable today. | `deepLink.test.ts` covers URL construction, token/error parsing, ignored links, stale links, and `setSession` outcomes. A physical email-client callback remains part of the P00-002/P09 device gate; no token-bearing URL is retained as evidence. |
| Network/offline and stale state | The app has no global connectivity observer, offline cache, or universal offline banner. Each data layer maps network failures to typed, calm screen-level error/retry states; screens distinguish initial blocking failure from a later refresh failure where applicable. Focus-gated bookings/messages channels refetch after reconnect and merge events idempotently. | Per-surface network/retry and realtime details are recorded in the trace tables above. Offline/reconnect performance and two-device proof remain P08/P09 work; the absence of a global state is documented behavior, not treated as an invented availability claim. |
| Permissions and destructive changes | Photo-library access is requested immediately before portfolio/verification upload; picker denial/cancel makes no database row. Location uses HTTPS forward geocoding rather than device-location permission. Native confirmation guards booking cancellation/rejection and destructive asset deletion; Bio additionally guards dirty navigation. | Permission and destructive controls remain source/unit covered but need physical permission and hardware-back evidence under P00-002/P09. Location dirty-draft asymmetry is separately tracked as RISK-025. |
| Selectors and test coverage | Every routable surface in the root/auth/customer/barber tables has a role-prefixed stable `testID` root plus enumerated static or dynamic selector patterns. Dynamic IDs encode only local row/room/message identifiers; they never embed messages, addresses, emails, document paths, or session tokens. Unit/component coverage is linked in each trace; authored Maestro paths are explicitly called out as unrun. | The inventory intentionally records selector patterns rather than recording live identifiers. Maestro is absent and no device is attached, so static inspection is not a claim of on-device selector usability. |

## Next inventory passes

1. Trace each screen’s rendered `Pressable`, button, form submit, and tab control to its handler and data source; classify it as functional, intentionally unavailable, or defect.
2. Record loading, empty, error, permission, offline, stale-session, and destructive-confirmation states per surface.
3. Extract every concrete test ID (including dynamic patterns), deep-link entry, and tests/maestro flow that covers it.
4. Reconcile this inventory with the P04 route target and RISK-014. `P00-004` is complete: all controls now have an evidence-backed action/data result; remaining hardware/device and waitlist work are separately blocked and do not reopen this documentation task.
