# CLAUDE.md — Project: Private Barber (working title)

This file is the authoritative project context. Read it fully before doing any work. Do not deviate from anything marked "authoritative" without explicit approval from the founders.

## Role and operating principle

You are acting as a senior full-stack engineer and architect for a two-person student founding team building a real product, not a demo. Do not blindly execute instructions — if something is legally risky, unrealistic for a two-person team, or a worse architectural choice than an alternative, say so and propose the better option before building. Work in small, tested increments, in the exact order defined in "Build order" below. Never skip the orchestration pipeline for any feature, no matter how small it looks.

## Product

An on-demand marketplace where customers book independent private barbers who travel to the customer's own location. Positioning: premium, private, trustworthy — not a generic barber-booking app, not a barber-pole aesthetic. Two completely separate mobile apps (Customer app, Barber app) sharing one Supabase backend. They must never share UI or navigation.

## Tech stack

- Frontend: React Native + Expo. One codebase, two separately branded and separately navigated apps (Customer, Barber).
- Backend: Supabase — Postgres, Auth, Storage, Realtime, Row Level Security. No self-managed servers.
- Payments: Stripe Connect — Phase 2 only, not in the initial build.
- Do not introduce any other backend service, database, or third-party API without explicit founder approval.
- The `database/supabase` MCP server is connected — use it for direct, live read/write access to the actual Supabase project instead of asking the founders to paste schema or data manually.
- The `react-native-best-practices` skill (Callstack) is referenced throughout this file but is NOT actually installed (verified 2026-07-09: absent from `.claude/skills/`, `~/.claude/skills/`, and the harness skill list — it was never invokable). Until it is installed (tracked in the backlog), apply its intent manually: RN/Expo performance, Hermes, bundle size, native module patterns.
- The `maestro-mobile-testing` skill (tovimx) is likewise referenced but NOT installed (same 2026-07-09 verification). The `.maestro/` flows written so far followed Maestro conventions manually and remain the house style — especially for optimistic UI updates and auth-gated screens, where flaky tests are most likely.

## Secrets and environment hygiene (authoritative — read before touching any Supabase key)

Supabase gives you two keys: the `anon` key (public, safe to ship inside the mobile app) and the `service_role` key (bypasses Row Level Security entirely — treat it like a master password). The `service_role` key must never appear in any React Native/Expo code, never be committed to GitHub, and never be used client-side under any circumstance. It only belongs in a server-side context (an Edge Function, if one is ever needed) and in environment variables that are gitignored. Before the first commit, confirm `.env` and any file containing keys is listed in `.gitignore`. If a key is ever accidentally committed, treat it as compromised and rotate it immediately in the Supabase dashboard — do not just delete the file from a later commit.

## Brand identity (authoritative — apply to every screen)

**Dark mode (default):** background `#121214`, card surface `#1B1B1E`, primary text `#F5F1E8`, secondary/muted text `#9A968C`, accent (brass) `#BFA06B` used sparingly — buttons, star ratings, verified badges, active states only, never as a large fill. Success `#51785C`, error `#A8453E`.

**Light mode:** background `#F8F4EC`, card surface `#FFFFFF` with a thin `#E6DFD0` border, primary text `#211D17`, secondary text `#8A8175`, accent text uses the darker `#8A6B3D` for contrast (fills/icons can still use `#BFA06B`). Success `#4F7355`, error `#A8453E`.

**Typography:** an editorial serif for headings, a clean sans-serif for body text. Sentence case, never all-caps or Title Case.

**Layout:** flat design — no gradients, no heavy drop shadows. Thin 0.5px borders/dividers. Generous whitespace. Calm, minimal, never cluttered.

## Roles

- **Customer** — browses barbers, books services, chats, rates after a completed booking.
- **Barber** — professional profile, sets prices and availability, accepts/declines bookings, uploads a portfolio (max 6 images).
- **Admin (the two founders)** — manually verifies barbers, handles disputes, approves/rejects accounts. Not a separate app — direct Supabase dashboard access is sufficient for Phase 1.

## Database schema (authoritative — only `supabase-schema-architect` may modify this)

- **USERS**: id, name, email, phone, role (customer/barber/admin), city, country, profile_image, created_at
- **BARBER_PROFILE**: id, user_id, bio, rating, verified (boolean), verification_status (pending/approved/rejected)
- **SERVICES**: id, barber_id, name, price, duration_minutes
- **AVAILABILITY**: id, barber_id, day_of_week (or specific_date), start_time, end_time
- **BOOKINGS**: id, customer_id, barber_id, service_id, date, time, location, price (snapshot of the service price at the moment of booking — never read live from SERVICES), status (pending/accepted/rejected/completed/cancelled), created_at — Realtime enabled on this table
- **CHAT_ROOMS**: id, booking_id, customer_id, barber_id
- **MESSAGES**: id, chat_id, sender_id, message, created_at — Realtime enabled on this table
- **PORTFOLIO**: id, barber_id, image_url — hard constraint: max 6 rows per barber_id
- **REVIEWS**: id, booking_id, customer_id, barber_id, rating, comment — only allowed if the linked booking's status is "completed"
- **VERIFICATION_REQUESTS**: id, user_id, id_image_url, license_image_url, status (pending/approved/rejected), reviewed_by, reviewed_at — no selfie field, no biometric data of any kind

## Booking state machine (authoritative — branching, not linear)

```
pending   → accepted   → completed → rated → archived
pending   → rejected
pending   → cancelled   (customer cancels their own still-pending booking — founder-approved 2026-07-09)
accepted  → cancelled
```

No other states exist. Every screen, query, and agent must respect exactly this branching. Actor rules are DB-enforced (migration 0011): only the barber may accept/reject/complete; only the customer may cancel a pending booking; either participant may cancel an accepted booking.

## Verification — manual only, no exceptions in Phase 1–2

Do not build any OCR or face-match service. No `face_recognition`, no selfie capture, no biometric processing of any kind — this is a deliberate decision made for legal/liability reasons (GDPR Article 9 special-category data), not an oversight. Flow: barber uploads ID photo + license photo → a founder reviews manually via the Supabase dashboard or a simple admin view → sets `verification_status` → barber appears in customer search only once `approved`. ID/license images live in a **private** Supabase Storage bucket, never public.

## MVP scope

**Build now:** auth for customer and barber roles, city-based barber discovery, barber profile pages, service creation, availability management, the booking flow end to end, realtime status updates, simple text chat attached to a booking, barber dashboard, portfolio upload (max 6), manual verification.

**Do not build yet, under any circumstance, without explicit founder sign-off:** payments/Stripe, subscriptions, AI-based recommendations, advanced analytics, multi-country/multi-currency logic, automated KYC or biometrics, push notifications.

**Deferred but tracked — do not let these silently disappear from scope:** a dispute resolution and refund/cancellation-fee policy (currently undefined — needed before real money moves in Phase 2), and in-home safety features for the barber-travels-to-you model (live location sharing during the appointment window, an SOS button) — flagged early as the single highest-liability part of this product if skipped indefinitely. Revisit both explicitly when planning Phase 2/3, do not assume they're solved.

## Build order

Work through these in order. Do not start a step before the previous one's test has passed. Exception (founder decision 2026-07-14): gates that require a physical two-device session (the Step 13-14 realtime gate, the Step 15-16 chat gate) are soft/environmental blockers, not hard sequencing rules — later steps may be scheduled while they remain open. They stay tracked in the backlog and must still pass before the Step 18 release gate.

1. Create the Supabase project and the Expo project; connect them.
2. Build the database tables listed above in Supabase.
3. **Test:** all tables exist and are visible in the Supabase dashboard.
4. Build the "Continue as Customer / Continue as Barber" screen.
5. Build customer signup and barber signup.
6. **Test:** one fake customer and one fake barber sign up successfully and appear in the database.
7. Build the barber's "add a service" screen and the availability screen.
8. **Test:** a service and an availability window are saved correctly.
9. Build the customer home screen (barber list by city).
10. **Test:** the test barber appears for the test customer.
11. Build the booking flow, sourcing available time slots from the barber's availability.
12. **Test:** a booking is created with status "pending".
13. Build the barber's incoming-requests screen with accept/reject.
14. **Test:** accepting updates the booking status instantly on the customer's side via Realtime.
15. Build simple text chat tied to a booking.
16. **Test:** messages send and receive correctly in both directions.
17. Build the manual verification admin flow.
18. Run the full flow end to end with real test users before moving to Phase 2.

## Orchestration pipeline (mandatory for every single feature, no exceptions)

Every feature passes through this exact sequence: **Plan → Design → Build → Validate → Secure → Integrate → Release.** Never combine more than one feature into the same build cycle — booking, chat, and verification are each their own pipeline run.

1. Use the `task-decomposition-expert` agent to break the feature into subtasks. No code is written at this stage.
2. Use the `architect-review` agent to validate the proposed design against the schema and state machine above.
3. If a schema change is genuinely required, only the `supabase-schema-architect` agent makes it, then run the `/supabase-schema-sync` slash command to push it live.
4. Use the `fullstack-developer` agent to build backend/API logic. No schema changes are allowed at this stage. For any feature involving Realtime (booking status updates, chat), also bring in the `supabase-realtime-optimizer` agent — a real agent as of 2026-07-09 (`.claude/agents/supabase-realtime-optimizer.md`), built around the realtime checklist from docs/design/step-13-14-realtime-requests-retroactive-review.md plus the F1/F2 lessons; supply it the live-verified publication/replica-identity/RLS facts its checklist marks [LIVE].
5. Use the `mobile-developer` agent to build the screens, applying React Native/Expo best practices manually (the `react-native-best-practices` skill is not installed — see Tech stack).
6. Use the `ui-ux-designer` agent (or the `ui-ux-pro-max` skill) to polish the UI against the brand identity section above.
7. Use the `test-engineer` agent for unit/integration tests, and author Maestro E2E flows following the conventions established in `.maestro/README.md` (the `maestro-mobile-testing` skill is not installed — see Tech stack) — the booking accept/reject flow and the chat flow are the highest priority for E2E coverage, since they involve realtime/optimistic UI updates where flakiness is most likely.
8. Use the `security-auditor` agent and run `/security-audit` plus `/supabase-security-audit`. This is the final gate — nothing is considered done without an explicit PASS. This check must also confirm no `service_role` key is present anywhere in client code.
9. If anything breaks at any stage, use the `debugger` agent. For Realtime-specific issues (a status or message not arriving live), check the Supabase MCP server's `get_logs` (service: realtime) and the live publication/replica-identity state first; for general data questions during debugging, query the live DB via the Supabase MCP server (`execute_sql`) instead of switching to the Supabase dashboard. (The previously-referenced `/supabase-realtime-monitor` and `/supabase-data-explorer` commands never existed in `.claude/commands/` — phantom references, corrected 2026-07-09.)
10. Once built and tested, use `database-optimizer` to check query performance and `context-manager` to confirm correct integration with everything already built, before starting the next feature.

**Important distinction:** everything in `.claude/agents/` is a subagent, invoked by describing the task in plain language — these are not slash commands. Only files actually present in `.claude/commands/` (for example `supabase-schema-sync`, `security-audit`, `generate-tests`) work as real `/slash-commands`. Skills that are actually installed (e.g. `ui-ux-pro-max`, `supabase-postgres-best-practices` in `.claude/skills/`) activate automatically when relevant or can be invoked directly — before referencing any agent/command/skill by name, confirm it exists in the corresponding directory (the 2026-07-09 inventory found five phantom references in this file alone).

## Hard rules

- Schema is sacred — only `supabase-schema-architect` touches it, ever.
- Security is the final, non-optional gate before any feature is considered complete, and explicitly includes a check that no `service_role` key has leaked into client code or git history.
- One feature per pipeline run. Never mix booking, chat, and verification work in a single cycle.
- Never skip task decomposition, even for changes that look small.
- No biometric or automated identity processing — manual verification only, until explicitly revisited in a later phase.
- Stay inside the MVP scope above. If asked to build something in the "do not build yet" list, flag it and wait for explicit confirmation rather than building it.
- Dispute resolution, refund policy, and in-home safety features are known open gaps, not solved problems — do not silently build around them as if they don't exist; surface them again when Phase 2 planning starts.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).

## Tasks (living backlog — maintained by Claude, read this every session)

Maintenance protocol (authoritative):
- This section lists every task still needed to build the app, in execution order. It is the single source of truth for "what's left".
- When a task is fully done (its test/gate passed), DELETE its line from this section in the same session — do not leave completed tasks behind.
- If a session is interrupted (session limit, crash, abandoned work), do NOT delete the task — append a short status comment beside it in the form `<!-- PROGRESS: what is done, what remains, where to resume -->`.
- When new work is discovered mid-feature (bugs, review findings, follow-ups), add it here as a task rather than keeping it only in conversation.
- Every feature task below still goes through the full orchestration pipeline (Plan → Design → Build → Validate → Secure → Integrate → Release). This list tracks WHAT is left, not a license to skip HOW.

### Step 5 — auth & signup (in progress)
- [ ] Stage 6 — test-engineer <!-- PROGRESS (2026-07-05, commit 29d84b4): DONE: jest-expo + testing-library infra added, unit/integration tests passing (authService error mapping + full idempotent insert-sequencing incl. 23505-as-success-of-other-writer on both users and barber_profile, errors.ts table-driven, useAuthShell phase transitions + remount-stability). tsc+eslint clean. Two Maestro flows authored (login->provisioning->role-landing, signup->await-confirmation->resend) but UNEXECUTED — no Maestro CLI installed yet. UPDATE (2026-07-06): Android dev-client tooling gap is now RESOLVED — adb/platform-tools installed and on PATH, phone authorized over USB, EAS Android development build succeeded and the APK is installed on the physical device via `adb install`, Metro running via `expo start --dev-client` with `adb reverse tcp:8081 tcp:8081`. The step-6 DB gate and the email-confirmation deep-link fix (commit 2997057) are both DONE and confirmed live on the device — see Build Order status, no longer tracked here. iOS dev client is still the founder's own `eas build --profile development --platform ios` (interactive Apple login, run from their own terminal, not this shell) — status unconfirmed, check `eas build:list` before re-triggering. Maestro CLI itself is STILL not installed on this machine — that is the one remaining tooling gap for running the two authored flows (on Android, at least; iOS needs its own device flow separately). REMAINS: (1) install Maestro CLI and point `maestro test` at the now-authorized Android device, (2) run the two authored flows and fix whatever breaks, (3) author + run the duplicate-email and app-kill-mid-provisioning flows (deferred on purpose until the first two run once). -->
- [ ] Composite/covering index on `users(city)` including join columns, so "approved barbers in city X" doesn't need extra heap lookups at scale (flagged by database-optimizer during Step 5 Stage 8 closeout, 2026-07-06). Not urgent at MVP scale — the other half of this original recommendation (partial index on `barber_profile.verification_status`) was applied 2026-07-06 as migration `0008_add_barber_profile_approved_partial_index.sql` once discovery queries actually went live during Step 9-10's Stage 10 closeout, and its urgency was reassessed upward at that point since it was now on three live customer-facing read paths (barber_directory, services_select_own_or_approved, availability_select_own_or_approved) rather than a queued/theoretical recommendation. Route through `supabase-schema-architect` whenever this one is actioned too.
- [ ] Anon leftover-privilege cleanup (flagged during Step 5 Stage 7, 2026-07-06): anon still holds `TRUNCATE`/`REFERENCES`/`TRIGGER` on all public tables — predates this app's own migrations (Supabase's default project bootstrap grants `ALL` to anon before migration 0001 ever ran); migration 0006 only revoked SELECT/INSERT/UPDATE/DELETE. Not reachable via PostgREST (doesn't expose those ops) so not an active exploit path, but worth closing for hygiene via a future `supabase-schema-architect`-owned migration. Optionally also consider moving `is_admin()`/`has_role()` to a non-PostgREST-exposed schema to silence the two low-risk WARN advisor findings (RPC-reachable by any authenticated caller, self-only fact-check, no cross-user data — not blocking).

### Brand mark — the Privelier Signet (founder-directed 2026-07-21)
Founders chose option 03 "The Signet" from their logo deck and supplied its fonts (Cinzel + Marcellus). SHIPPED (commit on main): `src/shared/components/Brandmark.tsx` (three lockups mark/vertical/horizontal, four named sizes, theme-aware, pure nested Views — NO react-native-svg, so no native rebuild), Cinzel-only logo tokens in `src/theme/typography.ts` (`fonts.logo`=Cinzel_400Regular wordmark, `fonts.logoSemiBold`=Cinzel_600SemiBold roundel P — LOGO-ONLY, Playfair stays the heading face because Cinzel is caps-by-design and the brand mandates sentence case everywhere else). Marcellus was evaluated and DROPPED (its only edge is a lowercase, useless for an all-caps logotype). Placed at the three thresholds the Ultra designer ranked and DELIBERATELY nowhere else (never tab bars/headers/avatars/empty states — over-placing a signet cheapens it): RoleSelect (vertical xl, replaced the bare Playfair wordmark), provisioning-loading, await-confirmation. 11 component tests; new `brandmark` testID documented in .maestro/README.md.
- [ ] Founder on-device visual review of the mark on all three screens (RoleSelect / provisioning wait / check-your-inbox), in BOTH light and dark. Ultra designer flagged the md vertical lockup (11px Cinzel) as the Android legibility edge — prefer lg for vertical where there's room. Verify the two on-device optical constants once at the largest size (the horizontal P nudge +1.5%, and confirm Cinzel's cap ≈0.700em so the P-size table holds; re-run the one formula if the measured cap differs).
- [ ] Remaining ranked placements NOT yet built (Ultra designer's plan, do not let them silently disappear): (a) the colophon — mark-only `sm` `decorative` at the very bottom of customer AccountScreen + barber StudioScreen, below sign-out, with a version line; (b) the app-icon tile + splash assets on a 1024px canvas (`assets/icon.png`, `android-icon-foreground.png`, `android-icon-monochrome.png` — thicken the inner ring there since monochrome drops the brass; `app.json` splash on `#121214`) — this is asset work, and the icon is the deck's ONLY sanctioned brass P; (c) OPTIONAL/founder-call: a small `sm` `decorative` mark at the bottom of the booking-confirmation screen (below the details, never above). The Marcellus TTFs the founders sent are archived in Downloads — the right answer IF a future secondary lockup ever needs a display face WITH a lowercase (e.g. a "Members" sub-lockup); not now.
- [ ] OPTIONAL hardening the designer suggested: a `no-restricted-syntax` ESLint rule forbidding any file other than Brandmark.tsx from importing `fonts.logo`/`fonts.logoSemiBold`, to mechanically enforce logo-only. A comment in typography.ts states the rule for now.

### UI rebuild against the Lovable prototype (founder-directed 2026-07-08, screen by screen)
Reference: https://github.com/Privelier/privelier-at-home.git (React web/Vite — visual/structural reference ONLY, never copy its code; Supabase schema/RLS/booking state machine stay untouched — this track rebuilds the UI layer only; each screen is shown to the founders before starting the next).
- [ ] Discover/Home screen <!-- PROGRESS (2026-07-08): rebuilt as src/customer/screens/DiscoverScreen.tsx (replaces CustomerHomeScreen) plus src/customer/components/BarberCard.tsx and a customer bottom-tab shell (src/customer/CustomerTabs.tsx: Discover/Explore/Bookings/Inbox/Account — Explore/Bookings/Inbox are placeholders, Account is a minimal profile+sign-out screen). Data stays real: batched listServicesForBarberIds() added to discoveryData.ts feeds "from €X" lines + dynamic service-name chips. Deps added (both JS-only, NO dev-client rebuild needed): @react-navigation/bottom-tabs, @expo/vector-icons; PlayfairDisplay_500Medium added to typography. tsc+eslint+jest all clean (43 customer tests pass). Maestro contract updated: customer-home-logout → customer-account-logout on the Account tab (both flows + .maestro/README.md updated); customer-home-screen/-loading/-error/-empty/-barber-{id} testIDs preserved. REMAINS: founder visual review on the physical device (phone was disconnected at rebuild time; Metro was left running). Founder decisions to confirm: EUR (€) currency display, static "Trending this week" editorial content (hardcoded Unsplash imagery), verified badge rendered for every directory row (approved ⇒ verified by construction). -->
- [ ] Barber profile detail screen <!-- PROGRESS (2026-07-08): rebuilt src/customer/screens/BarberProfileScreen.tsx to the prototype's customer.barber.$id layout — full-bleed 320px hero (profile image under status bar, flat scrim + light text, floating back button, StatusBar style=light only when an image exists), bio + service-name chips, Services/Portfolio/Reviews tab strip (sentence case per confirmed decision). Same data layer (getBarberProfile + listServicesForBarber). Data-honesty deviations: per-row Book button shows an Alert "Booking opens soon" until step 11-12; Portfolio tab = empty state until step 17; Reviews tab = rating + empty state until step 18; no description/distance/years lines (no such columns). formatMoney changed to exact cents (€42.50, never rounds) — affects Discover cards too. Maestro testIDs preserved (services is the default tab so existing assertions hold; -rating testID present in both rated and unrated branches); new IDs barber-profile-tab-{key} + barber-profile-book-{id} documented in .maestro/README.md. tsc+lint+jest clean. REMAINS: founder visual review on device (Metro left running; phone disconnected both times — Discover screen review is ALSO still pending, same session). Founder-confirmed decisions this session: € pricing, static Trending content, verified badge on all directory rows, sentence-case "Editor's pick". -->
- [ ] Bookings tab <!-- PROGRESS (2026-07-08): built src/customer/screens/BookingsScreen.tsx (replaces placeholder) per prototype customer.bookings — Upcoming/Past tab strip, booking cards. Real read path: new src/customer/bookingsData.ts fetchOwnBookingsView() (RLS bookings_select_participants scopes to own rows — verified live in pg_policies) + best-effort barber_directory/services enrichment with calm fallbacks. BookingRow/BookingStatus added to src/types.ts; formatBookingWhen added. Upcoming = pending/accepted AND slot >= now, else Past (isUpcomingBooking, unit-tested). Cards not tappable (booking detail = step 11-12). Empty states are the expected first render (no booking data exists until step 11-12). tsc+lint+jest clean (52). REMAINS: founder visual review. -->
- [ ] Inbox tab <!-- PROGRESS (2026-07-08): built src/customer/screens/InboxScreen.tsx (replaces placeholder) per prototype customer.inbox — header + subtitle, hairline-divided thread rows (avatar, barber name, preview, short date). Real read path: new src/customer/inboxData.ts fetchOwnInboxView() over chat_rooms + batched last-message scan (cap 200) + bookings/barber_directory/services enrichment; pure buildInboxThreads() (newest-message-per-room, activity sort, null-degrading lookups) unit-tested. ChatRoomRow/MessageRow added to src/types.ts; formatShortDate added (local-midnight anchoring for bare dates). Honesty deviations: no fake presence dot; preview = real last message, falls back to "About: {service}"; row tap → "Chat opens soon" Alert until step 15-16. Empty state expected (no rooms exist until chat ships). tsc+lint+jest clean (56). REMAINS: founder visual review. -->
- [ ] Account tab <!-- PROGRESS (2026-07-08): rebuilt src/customer/screens/AccountScreen.tsx to full prototype customer.account layout — profile row (avatar/initial, name, email, brass "Member" label), five hairline-divided settings rows (Favorites, Notifications, Privacy & security, Preferences, Help center) each navigating to a real AccountSection stack screen (new AccountSectionScreen.tsx, generic via section-key param, honest placeholder bodies; help section shows privelier@outlook.com as contact), destructive sign-out row (customer-account-logout testID preserved — login E2E flow depends on it). Founder-excluded, DO NOT ADD: Wallet & payments, Gift cards & credits rows. Deferred by design: stats strip (cuts/barbers/avg — all-zero noise until bookings/reviews data exists); customer verified badge removed (verification is barber-only). tsc+lint clean, full suite 194 tests pass. REMAINS: founder visual review (all three tabs of this batch: Bookings, Inbox, Account). -->
- [ ] Remaining customer screens, roughly in prototype order: booking flow (fold into step 11-12), booking confirmation, chat (fold into step 15-16); Account stats strip (cuts/barbers/avg rating) once bookings/reviews data exists

### Explore tab (founder-directed 2026-07-15: Mapbox confirmed over Google Maps) — two pipeline runs
Design gate for BOTH runs: docs/design/explore-location-design-approval.md (task-decomposition agent ran; architect-review applied manually — APPROVED WITH CONDITIONS C1-C9). Founder decisions locked: D2 exact coords internal-only, public pin = stored randomized 200-500m offset; D3 "House calls" chip repurposed to "Verified" (FLAGGED: no-op by construction — every directory row is approved ⇒ verified; founders should expect it to select everything until a differentiating tier exists); D4 no-location barbers appear in lists but never as fake pins. `EXPO_PUBLIC_MAPBOX_TOKEN` (public pk. token) added to .env (gitignored; value never in git history — verified).

Run A — barber location capture: SCHEMA+DATA+SCREEN BUILT 2026-07-15. Migration 0019 (`barber_location` table: exact address/lat/lng own-row-RLS-only — deliberately NOT on barber_profile, whose SELECT policy exposes approved rows to all authenticated; display_latitude/longitude = STORED-ONCE trigger-stamped offset, client values never survive, redraw only on coord change; barber_directory view + verified + display coords via LEFT JOIN) applied+ledgered+LIVE-PROBED (offsets measured in [200,500]m; customer sees 0 exact rows, pin only via view; advisors zero-new). App: src/shared/geocoding.ts (Mapbox v6, token never logged), src/barber/locationData.ts (writes ONLY the 4 app-owned columns — payload pinned by test), LocationEditScreen (BioEdit pattern; debounced geocode → picked-candidate-only coords; mandatory D2 consent copy; clear = pin removal), Studio Location card (barber-dashboard-location), dashboardData.locationAddress (NOT a readiness item — meter stays the founder-scoped 5). .maestro/barber-location-edit.yaml authored (unexecuted, same CLI gap). Optimizer/context intent applied: PK-only access + view PK LEFT JOIN need no new index; tsc app-wide clean; no import cycles; all testIDs additive.
- [ ] Run A founder on-device gate: barber types a real address on the phone → picks a candidate → saves; founder spot-checks in the dashboard that display_* differ from exact by a plausible offset (the 0019 trigger owns that — live probes already proved it server-side).
- [ ] Run A security gate = PASS (2026-07-15) — but applied MANUALLY in the main thread: three consecutive server-side API failures (529/529/500) killed the security-auditor agent mid-run, so the gate's checklist was executed inline against the live-probe evidence (offset privacy end-to-end incl. no reader-triggerable resampling channel; locationData sole accessor; payload/candidate-only-coords/token-never-logged/URL-encoding all pinned by tests; token value never in git history; zero new deps; service_role only as the deliberate 0019 grant; advisors zero-new). FOLLOW-UP owed: re-run the independent security-auditor agent over Run A when the API is stable, same scope. LOW findings tracked: (L1) geocode candidate labels are length-unbounded from the response — add a defensive slice if the geocoding provider ever changes from Mapbox; (L2 informational) the __DEV__-only geocode failure log includes the typed address query (house pattern, dev builds only); (accepted) a raw-API caller can self-attest arbitrary in-range coordinates for their OWN row — same trust class as any self-entered profile field, bounded by the 0019 CHECKs.
Run B — Explore tab UI: LIST-VIEW PATH BUILT 2026-07-15 (same session as Run A). src/customer/screens/ExploreScreen.tsx replaces the tabPlaceholders placeholder (file deleted, zero orphan refs); chips All / Available today / Under €100 / Verified (single-select; a chip whose backing batched read FAILED is HIDDEN, never silently filters everyone out); list/map toggle where Map renders an honest "arrives with the next app update" state (deliberate: @rnmapbox/maps was NOT installed — an installed-but-tokenless package fails every EAS build at the Gradle SDK-download step, so installation waits for the founder's download token); vertical BarberCard list, same Discover read chain (C9: fetchOwnProfile → listBarbersByCity → batched services + NEW batched listAvailabilityForBarberIds). Pure logic in src/customer/exploreData.ts — windowCoversToday (slots.ts specific_date-precedence semantics; DOCUMENTED APPROXIMATION: "has working hours today", not "has a free slot right now"), fromPrice, applyExploreFilter (narrow-only, C8), toMapPin (D4 as code: null display coords ⇒ null pin, pinned by tests) — 11 unit tests, pinned clock. Maestro flow customer-explore-filters-list.yaml authored (unexecuted, same CLI gap). Suite 492 pass / 1 known VerifyScreen fail; tsc+eslint clean. Security: zero DB change, zero new deps, zero service_role, filters narrow an RLS-authorized list only — inline gate PASS, folded into the same independent-agent re-run follow-up as Run A (agent infra was down).
Run B MAP INTEGRATION BUILT 2026-07-15 (founder provided the sk. download token same day): token stored as EAS secret `RNMAPBOX_DOWNLOAD_TOKEN` (visibility secret, all 3 environments — NEVER in .env/git, verified); `@rnmapbox/maps` ^10.3.2 installed; NEW `app.config.js` (dynamic config extending app.json) injects the config plugin ONLY when the env var is present — token never touches git, local `expo start` unaffected; note the plugin prop `RNMapboxMapsDownloadToken` is deprecated upstream in favor of the `RNMAPBOX_MAPS_DOWNLOAD_TOKEN` env name (app.config.js accepts both). Map pane: `src/customer/mapRuntime.ts` (`isMapNativeAvailable` = NativeModules.RNMBXModule check — the package THROWS at import when native is absent, so `components/ExploreMapView.tsx` is the ONLY importer and is require()d exclusively behind that check; never add a static import); ExploreMapView = Mapbox MapView (theme-aware Dark/Light styleURL) + Camera from pure tested `pinBounds` + price-pin MarkerViews (brass = active pin only; label = real from-price via formatMoney, barber name when no priced services) + docked BarberCard on pin tap (map-tap undocks, card-tap opens the profile); ExploreScreen's map area is a three-way branch (map-soon on the old dev client / map-empty when zero pins — no globe, D4 / the real map), pins derive from the FILTERED list so chips govern both views. Architect-review addendum APPROVED (see design doc — 3 LOW notes: attribution overlap while docked, blank tiles if the pk. runtime token were missing, allowOverlap fine at MVP density). Suite 495 pass / 1 known VerifyScreen fail; tsc+eslint clean; Maestro flow updated to the build-agnostic map-area assertion.
- [ ] EAS Android dev build a5726cbe FINISHED 2026-07-15 18:29 (Mapbox download token verifiably worked) and INSTALLED on the physical device same evening (`adb install` Success, R5CT625XMDL); Metro left running (`expo start --dev-client`, adb reverse set, .env loaded incl. the Mapbox pk token). REMAINS — founder on-device verification: Explore map renders (dark style), test barber's price pin at the OFFSET position (save an address via barber Studio → Location first if none saved), docked card on pin tap → profile nav, map-tap undocks, LocationEdit end-to-end, old flows smoke, and the architect addendum's L-a check (does the docked card cover the Mapbox logo? one-line logoPosition fix if so). Stable artifact URL if a reinstall is ever needed: https://expo.dev/artifacts/eas/fXopsLPlZWDiXM_pFPZ6H-iq2Rwo8AnWsSlwhlvlTxU.apk. iOS build remains the founder-run interactive flow (pre-existing gap).
- [ ] Run B founder on-device review (after the new APK installs): Explore list + chips + REAL map view (pin, docked card, profile nav), plus LocationEdit end-to-end (address → candidates → save → pin appears on Explore).
- [ ] Map follow-up nits from the architect addendum (LOW): logo/attribution overlap while a card is docked (check on-device, offset logoPosition if needed); pin clustering only if density ever demands it.
- [ ] Barber-side screens <!-- PROGRESS (2026-07-08): investigation done (prototype HAS a full 5-tab barber design: Studio/Requests/Portfolio/Chats/Verify; its requests state machine (confirmed/in_progress) does NOT match our authoritative one — ours wins; its verification flow writes verification_status from the client — forbidden here, admin-only). Iteration 1 DONE: BarberTabs.tsx 5-tab shell + StudioScreen.tsx (replaces BarberDashboardScreen) — greeting header, REAL verification-status line (new barber/profileData.ts fetchOwnBarberProfile, read-only), Services/Availability summary cards with real counts + from-price, founder decision: Services/Availability stay separate stack screens (NOT inline like prototype). testIDs barber-dashboard-logout/-services/-availability preserved (Maestro flows verified still valid); shared format helpers moved to src/shared/format.ts (customer/format.ts is a re-export shim). tsc+lint clean, 194 tests pass. Iteration 2 DONE (2026-07-08): RequestsScreen.tsx + requestsData.ts (real RLS-scoped bookings read, soonest-first, own-services lookup; BOOKING_STATUS_LABELS moved to shared/format.ts) — NO accept/reject actions (step 13-14) and cards lead with SERVICE name because users RLS blocks reading the customer's name (gap logged under Step 13-14). Iteration 3 DONE (2026-07-08): PortfolioScreen.tsx + portfolioData.ts (real RLS-verified read of PORTFOLIO — select open to authenticated, write own+barber-role; PortfolioRow added to src/types.ts; MAX_PORTFOLIO_IMAGES=6 mirrors the DB cap) — 2-col grid + "N of 6" counter live; add tile → "Uploads open soon" Alert; NO per-image delete yet (both upload+delete = step 17, expo-image-picker native module = new dev-client build). Iterations 4+5 DONE (2026-07-08): ChatsScreen.tsx + chatsData.ts (real chat_rooms/messages read; buildInboxThreads + InboxThread moved to src/shared/threads.ts, customer inboxData/types re-export; rows lead with service name + neutral avatar — same users-RLS counterpart gap; tap → "Chat opens soon") and VerifyScreen.tsx + fetchOwnVerificationRequest in profileData.ts (VerificationRequestRow added to src/types.ts; REAL status card from barber_profile.verification_status + REAL doc-row uploaded-state from own verification_requests row — both display-only, prototype's client-side status write deliberately NOT ported; upload buttons → "Uploads open soon", step 17). tsc+lint clean, 194 tests pass. ALL FIVE BARBER TABS BUILT. REMAINS: founder review of Chats+Verify, then the final barber iteration: Services/Availability restyle (keep CRUD, validation, and every Maestro testID). -->
- [ ] Auth + role-select screens restyled to the prototype look

STANDING DIRECTIVE (founder, 2026-07-13): the `ui-ux-designer` agent was rewritten into the "Ultra UI/UX Designer" (`.claude/agents/ui-ux-designer.md`) — RN/Expo-native, locked to the authoritative brand identity (the old generic advice to use gradients/colored shadows/avoid Inter was explicitly overridden by the brand), and built around reusable "ultra components". Use it for ALL new/changed design and component work from now on. First application (2026-07-13): the booking date/time calendar was rebuilt — new reusable `src/customer/components/CalendarDateStrip.tsx` (month-context header, "Today" marker, dimmed-not-hidden unavailable dates) + time slots grouped by morning/afternoon/evening + a selection summary above Continue; all testIDs and the availability/focus logic preserved; 341/341 tests pass. This is the reference bar for the end-of-project design pass (see Step 18 reminder).

### Step 7–8 — barber services & availability — gate PASSED on-device (2026-07-09, combined Steps 7-12 session, real device + real test accounts)
NOTE for any future direct-SQL admin write to barber_profile: migration 0005's trigger silently reverts verified/verification_status/rating back to old values unless `auth.role() = 'service_role'`; a raw Management-API SQL session doesn't carry that by default, so run `select set_config('request.jwt.claim.role', 'service_role', false);` first in the same query call, or use the Supabase dashboard's Table Editor instead (it sets this automatically).
- [ ] Barber-services E2E flows (`.maestro/barber-services-add-edit-delete.yaml`, `.maestro/barber-availability-add-edit-delete.yaml`) authored but unexecuted — Maestro CLI itself is still not installed (Android device/dev-client tooling is otherwise ready)

### Step 9–10 — customer discovery — gate PASSED on-device (2026-07-09, same combined session: approved barber visible in the customer's Discover list on-screen)
- [ ] Customer-discovery E2E flow (`.maestro/customer-discovery-view-barber-profile.yaml`) authored but unexecuted — same Maestro CLI gap as Step 7-8

### Step 11–12 — booking flow — CLOSED (2026-07-09)
Full pipeline run complete: design gate (docs/design/step-11-12-booking-flow-design-approval.md) → schema (migrations 0009, 0010) → data layer (`src/shared/slots.ts`, `src/customer/availabilityData.ts`, `src/customer/bookingCreateData.ts`) → screens (`BookingDateTimeScreen`/`BookingLocationScreen`/`BookingConfirmScreen`, wired from `BarberProfileScreen`'s Book button) → visual polish → tests (236/236 passing, unit+integration+authored E2E) → security gate PASS. Gate confirmed: a booking is created with status 'pending', price is server-stamped, and it now correctly requires the booked service to belong to the booked barber (migration 0010, closed a real price/attribution manipulation vector a raw-API caller could otherwise exploit — caught by the security audit, not by design review). Bookings tab (customer) and Requests tab (barber) both confirmed to refresh on focus (fixed a latent bug: both were mount-only `useEffect`, which would have hidden new bookings from an already-visited tab until app restart). ON-DEVICE CONFIRMED 2026-07-09: the full flow (date/time → location → confirm → pending row in `bookings` with server-stamped price → request visible on the barber's Requests tab) was run end to end on a real device with the two real test accounts, founder-verified — the gate is passed in reality, not just in mocked tests.

Tracked follow-ups (not blocking, do not let them silently disappear):
- [ ] Double-booking guard only catches identical `(barber_id, date, time)`, not overlapping durations — e.g. a 60-min booking at 10:00 and a 30-min booking at 10:15 for the same barber both insert successfully today (flagged by security-auditor 2026-07-09, non-blocking since the app UI's `deriveAvailableSlots` never offers an overlapping slot in normal use, but the DB is not actually authoritative for this case despite its own migration comment claiming so). Fix: a `btree_gist` exclusion constraint on the booking's actual time range, not just a point-equality index. Should land before Phase 2 real-money bookings make an accidental overlap costly.
- [ ] `AVAILABILITY` has no "closed"/blackout row type — a barber cannot express "normally open this weekday, but closed this one date," only additive `specific_date` overrides (design doc Decision 1, 2026-07-09). Fast-follow candidate, route through `supabase-schema-architect`.
- [ ] Customer-booking E2E flow (`.maestro/customer-booking-create.yaml`) authored but unexecuted — same Maestro CLI gap as Steps 6–10.

### Step 13–14 — barber requests & realtime status — core logic BUILT, gate not yet fully passed
Status honesty note (2026-07-09): the build stage of this step landed in commit `83989d2` BEFORE its pipeline stages ran — the founders directed a retroactive closeout instead of a revert. What is DONE and verified: migrations 0011 (actor-aware transition trigger, incl. the founder-approved pending→cancelled) and 0012 (get_booking_counterparts RPC — this RESOLVED the counterpart-identity gap the right way, without widening users SELECT) are live and audited; all four barber transitions + customer cancel + realtime hook/reducer + both screens are built; barber-side accept and complete were exercised on a real device 2026-07-09 (the on-device test booking went pending→accepted→completed); retroactive design review APPROVED (docs/design/step-13-14-realtime-requests-retroactive-review.md); unit tests added (applyBookingChange reducer, all five transition mutations, fetchOwnRequestsView — suite now 266); accept/reject Maestro flow authored (`.maestro/barber-requests-accept-reject.yaml`); /security-audit + /supabase-security-audit PASS (2026-07-09) over 0011/0012 + the new mutation paths, service_role clean in tree and full history.
- [ ] Gate: accepting updates the booking status instantly on the customer's side via Realtime — needs a deliberate two-device (or device + second session) check with both test accounts; the single-device Maestro flow cannot cover this half and says so in its header. Review findings F1 (reconnect-gap refetch via `onRecovered`) and F2 (reference-preserving sorted merge) were FIXED 2026-07-09 (unit-tested, incl. the full recovery matrix — suite 285), so this same session should also sanity-check the reconnect path: toggle airplane mode mid-focus, change the booking from the other side, re-enable network, and confirm the list catches up without a tab switch. (Soft/environmental blocker per founder decision 2026-07-14 — this open gate does not block scheduling later steps; it must still pass before the Step 18 release gate.)
- [ ] Run `.maestro/barber-requests-accept-reject.yaml` once Maestro CLI is installed (same gap as Steps 6–12; it needs exactly two seeded pending bookings — see the flow's header).

### Cross-cutting hygiene (discovered 2026-07-09 during the Step 13-14 closeout and the same-day agent inventory)

NPM-AUDIT BASELINE MOVED 2026-07-21 — earlier entries below and above say "the 11 known build-tooling moderates"; that figure is now STALE, and a future security gate should not read the change as a regression it caused. Current baseline: **12 moderate + 1 high**. The high is `brace-expansion` (DoS via exponential-time expansion), reached ONLY through `@testing-library/react-native` → `jest` → `glob` → `minimatch` — dev/test tooling, never bundled into the app. The moderates are the same pre-existing Expo BUILD-tooling chain as before (`uuid` via `xcode`/`@expo/config-plugins`, now also surfacing through `@rnmapbox/maps` → `expo`). Verified NOT caused by the same-day Cinzel/Marcellus install: both `@expo-google-fonts/*` packages have ZERO dependencies (`{}` for both `dependencies` and `peerDependencies`). Nothing here is runtime-reachable; re-baseline this line rather than re-investigating from scratch.
- [ ] VERIFICATION DOCUMENT SUBMISSION WAS BROKEN LIVE — FIXED 2026-07-17, gate still open. **The 2026-07-15 entry's diagnosis and proposed fix were both WRONG; do not act on any copy of them.** It claimed "0015 missed the UPDATE grant, first-time submission works, fix = `GRANT UPDATE ON public.verification_requests TO authenticated`". Reality, verified live 2026-07-17 against the real DB (not mocks): submission NEVER worked at all — first-time OR resubmission — and `verification_requests` had **0 rows**, confirming no barber has ever submitted successfully. TWO stacked defects: (1) `submitVerificationDocument` used `.upsert({user_id, [col]: path})`, which PostgREST compiles to `ON CONFLICT (user_id) DO UPDATE SET user_id = excluded.user_id, ...`; `authenticated` holds column-level UPDATE on ONLY (id_image_url, license_image_url) — 0015 section 6's deliberate "column-grant guardrail" — never on `user_id`, and Postgres checks DO-UPDATE column privileges at STATEMENT level even when no conflict row exists ⇒ every call died 42501. FIXED app-side (commit below): `submitVerificationDocument` is now explicit update-then-insert, never an upsert — the UPDATE arm sends ONLY the granted image column (never `user_id`), the INSERT arm supplies user_id + the image column, 23505 retries the update once, and a still-unmatched retry is mapped `forbidden` (an already-approved row is out of `update_own`'s pending/rejected scope). Proven live as a real authenticated barber: the update landed and the 0015 re-queue trigger correctly flipped status rejected→pending. (2) both `id_image_url` and `license_image_url` are NOT NULL with no default (0001:147-148) while `VerifyScreen.handleUpload(docType)` uploads ONE doc at a time ⇒ the first single-doc INSERT died 23502, so no row could ever exist (which also made defect 1's resubmit path unreachable). Fixed by migration 0020 (founder-approved 2026-07-17: drop NOT NULL on both image columns — a request may hold one doc while the other uploads; matches 0015's independent per-column re-queue comparisons and VerifyScreen's per-doc uploaded state). **WHY THE MOCKED TESTS PASSED THROUGHOUT: `verificationData.test.ts` mocks the Supabase client entirely, so it cannot see a Postgres privilege or a NOT NULL constraint — the 2026-07-13 "tests passed" claim proved only payload shape. The suite now pins the UPDATE payload as never containing `user_id` (the exact regression), but a mocked suite structurally cannot gate this class of bug; the on-device Step 17 gate is what actually proves it.** DO NOT EVER apply Postgres's own error HINT here (`GRANT UPDATE ON public.verification_requests TO authenticated`) — that is where the wrong fix came from, and it is a privilege escalation: `verification_requests_update_own`'s WITH CHECK does not pin `status`, and the re-queue trigger ignores a status-only update, so table-level UPDATE lets a barber self-approve their own request and forge reviewed_by/reviewed_at (not a customer-visibility escalation — `barber_directory` gates on the trigger-protected `barber_profile.verification_status` — but it corrupts the founders' manual review queue, i.e. the legal safeguard). REMAINS: the Step 17 on-device gate below (upload → dashboard approve → Discover) is now genuinely runnable for the first time; run it before considering this closed. Also note anon still holds the bootstrap TRUNCATE/REFERENCES/TRIGGER junk here (same pre-existing hygiene item as the other tables).
- [ ] Freeze `verification_requests.reviewed_by` / `reviewed_at` against client writes (raised by `supabase-schema-architect` while authoring 0021, 2026-07-21 — NOT closed by it). 0021 pinned `status` in the WITH CHECK, which stops self-*approval*, but the policy still does not pin the two reviewer columns: if anyone ever adds the table-level UPDATE grant that Postgres's 42501 HINT keeps suggesting, a barber could forge `reviewed_by = <a founder's uuid>` + `reviewed_at = now()` while leaving `status` at pending/rejected — the row then reads as reviewed-and-bounced by a named founder, which is still corruption of the manual review queue (the legal safeguard). So the column grant, not the policy, remains the real guarantee today. RLS cannot express column immutability, so this needs a TRIGGER and its own run; the architect deliberately did not bolt it onto 0021 because pinning those columns in the policy would forbid the founders' own dashboard writes if the review path ever moves off `service_role` (see the tracked single-approval-RPC item) — that is a founder design decision, not the architect's.
- [ ] `fullstack-developer` agent's body describes the wrong stack (Next.js 15/React 19, tRPC/Hono, Drizzle, Vercel — none used here). It has worked because task prompts carry the real Expo+Supabase context, but its priors pull web-stack; rewrite its Focus Areas/Approach for this project's actual stack. (Founder-deferred 2026-07-09 — noted, not yet fixed.)
- [ ] `debugger` agent pins `model: claude-sonnet-4-5`, a previous-generation model id — update or remove the pin. (Founder-deferred 2026-07-09 — noted, not yet fixed.)
- [ ] `architect-review` and `supabase-realtime-optimizer` have `.claude/agents/*.md` definitions but are NOT spawnable subagent_types via the Agent tool (harness's registered types this session omit both; confirmed 2026-07-14 when a spawn of `architect-review` returned "Agent type not found"). Their gates have been run by applying the .md definition manually in the main thread (architect-review did this for the barber-dashboard design gate, 2026-07-14). To make them truly spawnable they likely need registering wherever the harness sources agent types (not just the .md presence). Until then, invoke their intent manually.
- [ ] Actually install the two skills CLAUDE.md long claimed were installed but never were (corrected 2026-07-09): `react-native-best-practices` (Callstack) and `maestro-mobile-testing` (tovimx). Until then their intent is applied manually (see Tech stack section).
Migration-ledger note (RECONCILED 2026-07-21, kept for provenance): 0001/0005/0009/0010 were applied via raw SQL, never `apply_migration`, so the ledger never recorded them; they now have entries with SYNTHETIC but order-preserving versions (20260703120000, 20260705220000, 20260709140000, 20260709141000) that sit between their real neighbours. `statements` is deliberately NULL on those four — we have no trustworthy record of the exact text applied and inventing one would be worse than an honest gap; the files in `supabase/migrations/` are the record. The integrity-CHECK migration's ledger name was also corrected from `0017_` to `0018_add_integrity_check_constraints` (version 20260715021023) to match its file and stop the two 0017s reading as duplicates. `supabase db push` now sees all 21 in true order.
- [ ] Security-advisor items, none blocking: `barber_directory` is a SECURITY DEFINER view (advisor ERROR — intentional-by-design cross-user read surface, same rationale as the narrow RPCs, but document the acceptance or convert to `security_invoker` via `supabase-schema-architect`); leaked-password protection is disabled in Supabase Auth (WARN — one dashboard toggle, founder action); the two new RPC WARNs (`get_barber_busy_slots`, `get_booking_counterparts`) are the deliberate, documented pattern — accepted.
- [ ] `VerifyScreen.test.tsx` fails in FULL-suite runs via cross-suite pollution (passes 5/5 in isolation — RNTL async-act env degradation, same class as the repo's no-multi-mount convention for focus-effect screens). The date-fragility half of this item was FIXED 2026-07-15, commit a089bca (clock pinned via jest fake timers in `slots.test.ts` AND `dashboardData.test.ts` — the latter was a second instance of the same class, fixture dated 2026-07-15 written as "+1 day" on 07-14, expired the next day; both suites are now deterministic on any run date). Full suite baseline now reads 460 pass / 1 fail = only this VerifyScreen failure.

### Step 15–16 — chat — core BUILT (2026-07-10), gate not yet passed
Built and reviewed: migration 0013 (AFTER INSERT trigger on bookings creates the chat_rooms row at booking time per the founder's 2026-07-09 decision — chat opens while still pending; idempotent ON CONFLICT, backfill for pre-existing bookings, composite messages(chat_id, created_at) index replacing the single-column one; applied via the migration API so the ledger is consistent). Conversation screens for BOTH apps (message list, input, send; honest "Sending…"/"Not sent — tap to retry" pending bubbles via the shared `useSendQueue` hook — no fabricated rows; read receipts and a typing indicator were originally out of scope but were ADDED by the 2026-07-14 founder decision, see their subsection below; presence remains unbuilt) wired from the Inbox/Chats thread rows (both lists now load on focus); barber header upgrades to the customer's name via the 0012 RPC. Realtime copies the step 13-14 architecture verbatim: `messageRealtime.ts` reducer + `useMessagesRealtime.ts` per-room channel incl. the F1 onRecovered recovery and F2 reference bailouts. The `supabase-realtime-optimizer` agent's FIRST real run (2026-07-10) returned APPROVED WITH FINDINGS; its M1 (synchronous double-submission guard on send/retry — React state commits too late and duplicate messages are permanent) was fixed same-day and pinned by useSendQueue tests. Suite 325/325; post-migration security advisors unchanged (no new findings). Maestro flow `.maestro/customer-chat-send-message.yaml` authored (post-echo assertions), unexecuted — same CLI gap.
- [ ] Gate: messages send and receive correctly in both directions — two-device (or device + second session) check with both test accounts; single-device Maestro covers only the send half. During this same run, do the review's L1 one-time check: log one realtime payload's `created_at` next to the fetched row's — if PostgREST and Realtime serialize timestamptz differently, every own-send echo misses the F2 no-op bailout (and briefly renders a wrong clock); fix by normalizing `created_at` in `useMessagesRealtime`'s payload normalization if and only if they differ. (Soft/environmental blocker per founder decision 2026-07-14 — this open gate does not block scheduling later steps; it must still pass before the Step 18 release gate.)
CHAT LENGTH COUPLING (added 2026-07-19, closing a real regression — do not undo): `src/shared/messageLimits.ts` MAX_MESSAGE_LENGTH=2000 MIRRORS the DB constraint `chk_messages_message_len` (migration 0018) and is applied as `maxLength` on BOTH composers. If either side of that pair ever changes, change both in the same commit — `messageLimits.test.ts` pins the constant so a one-sided edit fails loudly. WHY: 0018 shipped the CHECK with no client cap, so a pasted >2000-char message failed 23514, `useSendQueue` discarded the reason, and the "Not sent — tap to retry" bubble re-sent identical text forever. The send queue now carries `failureMessage` (already-mapped copy, never raw server text) to the bubble and clears it on retry. Found by the bio-edit T8 security gate, fixed same day.
- [ ] Review follow-up L2 (LOW): `fetchConversation` fetches the whole room history on every focus AND every F1 recovery — fine at MVP (one room per booking, index-ordered scan) but payload grows monotonically in a long-lived room. Follow-up: `.order(desc).limit(~200)` + reverse (fits the inverted list). Do not let this silently disappear.

Unread indicator (founder-directed 2026-07-10) — BUILT same day: migration 0014 `chat_read_state` (own-row RLS, live-probed: counterpart cannot read it, spoofed writes rejected — security-audit PASS), design note docs/design/step-15-16-unread-indicator-design.md, app-level unread provider per app (unfiltered RLS-scoped messages channel), Inbox/Chats tab badges (count), bold+dot unread thread rows, mark-as-read on opening the specific conversation. supabase-realtime-optimizer review #2: APPROVED WITH FINDINGS — M1 (hook tests) and M2 (baseline now loads on SUBSCRIBED, never before the join) fixed same day, plus L1 (no marker write for own sends). Suite 341/341. Remaining follow-ups from that review, LOW, tracked here:
- [ ] Unread review L2: a recovery baseline racing a still-in-flight read-marker upsert can transiently re-flag a just-left room until the next successful baseline (self-healing). Fix idea: track in-flight marker writes and suppress re-flagging until they settle.
- [ ] Unread review L3: device clock behind server + a room's newest message outside the 200-message scan cap can re-flag an already-read room after restart (documented in the design note §4). Optional fix: conversation screens feed their fully-loaded newest message back to the provider.
- [ ] `service_role` has no DML grants on `chat_read_state` (side effect of the strict revoke in 0014) — harmless today (no server-side path touches it), note for future admin tooling; `supabase-schema-architect`-owned if ever needed.
- [ ] Open product question (independent design review 2026-07-11): the `messages_insert_participants` policy is participant-only with NO booking-status predicate, so a participant can still post into the thread of a rejected/cancelled/archived booking. This is consistent with the 2026-07-09 founder decision that chat opens at booking (pending) and is likely intentional, but the terminal-state behavior isn't explicitly recorded — confirm "messaging stays open regardless of final status" is intended, or add a status predicate (and a matching read-only UI state) if not. Not a defect until the founders decide.

Read receipts + typing indicator (founder decision 2026-07-14, reversing the original no-receipts scope) — BUILT: WIP commit 9310888 (the interrupted session's emergency commit; never reached origin until 2026-07-15), landed via merge ea6ec96, pushed. Contents: migration `0017_read_receipts_and_typing_broadcast` (LIVE since 2026-07-14; widened chat_read_state SELECT to room participants, added chat_read_state to the realtime publication, first broadcast authz in the codebase — private `typing:{chat_id}` policies on realtime.messages), design doc docs/design/chat-read-receipts-typing-design-approval.md, `src/shared/readReceipts.ts` + `useReadReceipt.ts` (counterpart last_read_at, monotonic reference-preserving merge) + `useTypingBroadcast.ts` (private:true + self:false, 2.5s leading-edge throttle, 3s trailing stop, 5s receiver TTL), AppState foreground gate in `useUnreadThreads` (no pocketed-phone fabricated receipts), wired in both ConversationScreens; `.maestro/customer-chat-read-receipt.yaml` authored (unexecuted — same CLI gap). RETROACTIVE GATES RUN 2026-07-15 (the feature had shipped without them): supabase-realtime-optimizer agent = APPROVED WITH FINDINGS (its M1, the then-unresolved merge, was already satisfied — merge ea6ec96 preserved the `.eq('user_id')` guard, pinned by useUnreadThreads.test.ts:186); security-auditor agent = PASS; /security-audit-equivalent clean (no service_role in tree or full history, zero new deps, npm audit unchanged at the 11 known build-tooling moderates); /supabase-security-audit-equivalent clean (advisors = exactly the six known/accepted items, ZERO new from 0017/0018). Follow-ups:
- [ ] Security M1 — REWORDED 2026-07-15 after the on-device session (IMPORTANT CORRECTION): do NOT blanket-enable "private channels only" in Supabase Realtime settings — that setting would break EVERY existing standard channel in the app (the bookings/messages/chat_read_state postgres_changes subscriptions and the app-level unread channel are all non-private). The M1 verification must be the narrow form: live-verify that a PUBLIC (non-private) join of `typing:{chat_id}` is rejected by the 0017 policies' absence for standard channels — or migrate ALL app channels to private first and only then flip the toggle. RELATED OPEN QUESTION (same session): the customer app showed PERSISTENT `CHANNEL_ERROR` on `messages:unread:{uid}` (6+ occurrences, Metro log) — if a founder already flipped "private channels only" after reading the original M1 wording, that toggle is the likely cause and should be reverted; otherwise investigate via realtime logs during an active session. Until resolved, live unread badges and the read-receipt baseline (M2-gated on SUBSCRIBED) are both non-functional in live use.
- [ ] Realtime L1: stale/contradictory marker rationale — `src/shared/readReceipts.ts:11-14` and design doc D3 claim "max(device now, newest message created_at)" but the implementation is server-timestamp-only (`unread.ts:59-76`); correct both comments.
- [ ] Realtime L2: customer ConversationScreen header (`src/customer/screens/ConversationScreen.tsx:23-24`) still says "No read receipts, no typing indicator" directly above the code rendering both — update it (barber header is fine).
- [ ] Realtime L3: `useUnreadThreads.ts` marker-source map string-compares `created_at` across PostgREST/Realtime serializations; since 0017 that map feeds a counterpart-visible receipt — switch to `parseTimestampMs` when next touched (fold into the step 15-16 L1 timestamptz check above).
- [ ] Realtime L4 (test gaps): (a) typing send-suppression during an outage (SUBSCRIBED → CHANNEL_ERROR → notifyActivity → expect no send) is untested; (b) neither new hook has an explicit roomId-switch teardown test (only enabled-toggle). L5 cosmetic: `useReadReceipt.mergeIncoming` allocates on a malformed event when prev is null — one spurious re-render.
- [ ] Security L2: five ungated `console.warn`s of channel-name+status across the realtime hooks (house pattern; tighten all five together if ever). Security L3: no server-side typing rate limit (client throttle only; Supabase tenant Realtime limits are the backstop) — accepted at MVP.
- [ ] Gate (two-device, soft blocker like the rest of step 15-16): receipts + typing deliver live on hardware in both directions (design D6) — fold into the same two-device session as the chat gate above; must pass before the Step 18 release gate.

### Step 17 — manual verification flow (one pipeline run)
Barber-upload half DONE (commits a0c0114→7e415fa): migration 0015 (private `verification-docs` bucket + storage RLS + resubmit re-queue trigger), upload data layer + `expo-image-picker`, Verify-tab upload wired/polished, security fix (unique per-upload object path, dropped upsert). 359 tests pass. Founder review path is dashboard-only (founder decision 2026-07-13) — procedure documented in `docs/design/step-17-founder-review-path.md`, discovery chain verified live (approved `barber_profile.verification_status` ⇒ `barber_directory`, the sole gate).
- [ ] Founder review path GATE: run one real approval end to end on-device — barber uploads via Verify tab → founder approves in the Supabase dashboard per the doc → barber appears in the customer's Discover. (No app build required; this is a live-data + procedure verification, not code.)
- [ ] Two-status-surface drift (flagged 2026-07-13, non-blocking): no trigger links `barber_profile.verification_status`/`verified` to `verification_requests.status`/`reviewed_by`/`reviewed_at`; the dashboard flow must set both by hand, and the resubmit trigger re-queues only the request, not the profile. Long-term fix = a single `service_role`-owned approval path (RPC/Edge Function) writing both tables atomically. `supabase-schema-architect`-owned when actioned.
- [ ] Portfolio upload (max 6 images, enforce the 6-row constraint) — barber profile <!-- PROGRESS (2026-07-13, committed+pushed as ebc423a): pipeline ran stages 1-6 + partial 7. DONE & LIVE: design note docs/design/step-17-portfolio-upload-design-approval.md (D1 PUBLIC bucket — founder REVERSED an initial private+signed choice; D2 store PATH, derive getPublicUrl); migration 0016 (PUBLIC `portfolio` storage bucket + owner-folder+barber-role INSERT/DELETE storage RLS; SELECT open via public bucket) APPLIED LIVE + ledgered, zero new advisors; data layer src/barber/portfolioData.ts (uploadPortfolioImage/insertPortfolioRow[maps the max-6 P0001 trigger error to 'limit_reached']/deletePortfolioImage[DB row FIRST then best-effort storage remove, orphan accepted]), src/shared/portfolioImages.ts (getPublicPortfolioUrl), src/customer/discoveryData.ts (listPortfolioForBarber), mapStorageError extracted to src/barber/errors.ts; screens PortfolioScreen (upload/delete, max-6 gate, double-submit busyRef, uploading tile shows the actual picked image) + BarberProfileScreen Portfolio tab; Ultra design pass IMPLEMENTED — shared src/shared/components/PortfolioTile.tsx + PortfolioGrid.tsx. Stage 7 tests DONE (commit ef87908): all 5 portfolio suites pass (49 tests) — the 9 interrupted component tests were fixed (root cause was RNTL v14's async render(), NOT product bugs — see [[rntl-async-render-testing]]); tsc+eslint clean. Stage 8 SECURITY GATE PASSED (2026-07-14): security-auditor agent (verdict PASS) + /security-audit + /supabase-security-audit all run. CONFIRMED LIVE: no service_role anywhere in client code/.env/git history (.env holds only the anon key — JWT role=anon — and is gitignored); RLS enabled on both public.portfolio and storage.objects; storage RLS truly blocks barber A writing/deleting barber B's folder (portfolio_insert_own/portfolio_delete_own predicate `(storage.foldername(name))[1] = auth.uid() AND has_role('barber')`), and table portfolio_write_own keys off `barber_id = auth.uid()` — both boundaries share ONE identifier since portfolio.barber_id FK→users.id=auth.uid(); anon has NO DML on portfolio; migration 0016 introduced ZERO new advisors (only the known/accepted barber_directory-view + 4 SECURITY-DEFINER-fn + leaked-password items remain). Four LOW/informational findings, none blocking, tracked as follow-ups below (L1 image_url folder unbound, L2 non-CSPRNG object names, L3 orphan readable after best-effort delete, L4 P0001 error-copy fallthrough; +L5 no bucket MIME/size cap). REMAINS to fully close: only (2) run the authored .maestro/barber-portfolio-upload-delete.yaml when Maestro CLI lands (same CLI gap as every prior step). NOTE: full suite now "381 pass / 12 known-pre-existing fails" (slots.test.ts date-fragility + one VerifyScreen slots-pollution — logged under Cross-cutting hygiene); no portfolio test is among the fails. -->
- [ ] Portfolio security follow-ups (all LOW/non-blocking, from the 2026-07-14 stage-8 gate — do not let them silently disappear): (L1 FIXED 2026-07-15: the folder-bound CHECK landed in migration 0018 alongside the `messages.message` and `barber_profile.bio` CHECKs — all three live-verified.) (L2) object names use `Math.random()`, not a CSPRNG — on a PUBLIC bucket the filename is the only barrier for unauthenticated callers; prefer `expo-crypto` randomUUID/getRandomValues (also applies to verificationData.ts's identical helper, though that bucket is private so lower urgency). (L3) a best-effort storage-delete failure leaves the object world-readable at its public URL after the row is gone (accepted D4 orphan tradeoff — documented; a later reconciliation sweep would close it). (L5) no bucket-level MIME/size cap — a barber could store arbitrary bytes in their own public folder (own-folder + barber-role + max-6 rows bound the abuse); consider storage bucket file-size/MIME restrictions. L4 (P0001 error-copy fallthrough surfacing booking copy on the Portfolio screen if the max-6 trigger message text ever changes) is a robustness nit, not security — fix in `insertPortfolioRow`/`mapPostgrestError` if that error mapping is next touched.
- [ ] Barber dashboard (bookings overview, profile completeness) — Step 17's last sub-feature <!-- PROGRESS (2026-07-14): pipeline STARTED. Stage 1 (task-decomposition) + Stage 2 (architect-review) DONE. This ENHANCES the existing Studio tab (src/barber/screens/StudioScreen.tsx), NOT a new screen. FOUNDER DECISIONS LOCKED (via AskUserQuestion 2026-07-14): (a) profile completeness = 4 items — >=1 service, >=1 availability window, >=1 portfolio image, verification — BIO DESCOPED (no bio-edit screen exists; bio-edit tracked as its own new backlog line below); (b) readiness framing — verification-PENDING renders as a calm in-progress state, never a red 'missing/fault'; meter is 'readiness to go live', not a score. Also adopted (sensible defaults): Studio switches to refresh-on-focus (matches rest of app); 'pending requests' tap deep-links to Requests tab; NO earnings/analytics (out of MVP scope). Bookings overview = READ-ONLY glance from the SAME fetchOwnRequestsView() the Requests tab uses (pending count, next appointment = earliest FUTURE accepted booking, upcoming-7-days count); NO mutations (Requests tab stays sole mutation surface). Architect-review verdict = APPROVED WITH CONDITIONS: C1 dashboardData.ts must NOT import from src/customer/ — extract the {date}T{time} slot composition (currently in src/customer/bookingsData.ts isUpcomingBooking) into src/shared/ so both apps share one impl; C2 next-appointment = status==='accepted' && future (NOT pending); C3 read-only glance + focus-refresh, NO second realtime subscription (Requests owns useBookingsRealtime); C4 minor duplicate services fetch acceptable at MVP, defer to database-optimizer; C5 pure degrade-per-field derivations, no client-side authz. NOTE: architect-review agent .md exists but is NOT a spawnable subagent_type (harness gap, logged under Cross-cutting hygiene) — the gate was performed manually applying its definition. STAGE 3 (data layer) DONE: src/shared/bookingTime.ts (bookingSlotStart + isUpcomingBooking — C1; customer/bookingsData now re-exports it, behavior-preserving); src/barber/dashboardData.ts (pure deriveBookingsOverview [pending count, accepted-only future next-appt sort-independent, upcoming-7-day count, best-effort name resolution] + deriveProfileReadiness [4 items, verification pending=in_progress/rejected=attention/approved=complete, isLive] + fetchDashboardView orchestrator, degrade-per-field, never fails whole dashboard); dashboard view/readiness types in src/barber/types.ts. tsc+eslint clean; 23 new unit tests; full suite 414 pass / 1 fail (only the known VerifyScreen cross-suite-pollution fail, passes in isolation — slots.test.ts date-fragility did NOT bite on 2026-07-14). STAGE 4 (mobile screen) DONE: StudioScreen.tsx extended — read-only bookings-overview glance (pending pill, next-appointment line via formatBookingWhen, upcoming-7-days line; taps deep-link to the Requests tab) + profile-readiness meter (flex progress bar + 4 item rows; each incomplete item deep-links to Services[stack]/Availability[stack]/Portfolio[tab]/Verify[tab]; complete items inert; verification in_progress renders calm). Switched to refresh-on-focus (useFocusEffect; spinner only on first load, later focuses refresh silently, a failed refresh keeps the last good view). All 7 preserved testIDs kept + new testIDs barber-dashboard-overview / -readiness / -readiness-{services|availability|portfolio|verification}. New src/barber/screens/__tests__/StudioScreen.test.tsx = ONE comprehensive mount asserting the 7 testIDs + overview + readiness states + both deep-links (deliberately single-mount: this codebase does NOT component-test useFocusEffect screens — RNTL's async-act env degrades after several such mounts in one file; verified at length 2026-07-14; the derivation logic is fully covered by dashboardData.test.ts and screen wiring by the Maestro flow). tsc+eslint clean; full suite now 415 pass / 1 known VerifyScreen fail. STAGE 5 (Ultra UI polish) DONE 2026-07-14: ui-ux-designer (Ultra) agent reviewed both new sections and returned a 9-issue implementation-ready spec, ALL implemented in StudioScreen.tsx (~60-line diff, no new deps, all testIDs verbatim): (1) readiness card marginTop 12 (was 0 — rhythm bug); (2) readiness rows now 44px min-height hairline-divided list rows (WCAG 2.5.8; last row borderless); (3) pending pill + chevron now COEXIST (nav affordance no longer vanishes when pending>0; dynamic a11y label); (4) next-appointment upgraded to "Next appointment" caption + two-tone baseline row (name bodyMedium primary ellipsizing, time body secondary never truncating); (5) continuous brass bar REPLACED by 4 discrete 3px segments mirroring the 4 rows (brass=complete, border=not; hidden from a11y tree — status text carries the count; "readiness not a score" made literal); (6) done rows recede (muted icon+label, regular font — green checks removed; error color reserved for 'attention' icon only; brass rationed to pill+segments only); (7) is-live = the screen's SINGLE green moment (check-circle + green bodyMedium line replacing the count line; all rows/testIDs kept in every state); (8) pressed-opacity 0.85 on every Pressable (overview, link cards, readiness rows); (9) copy: "{n} upcoming in the next 7 days" (was bare "{n} in..."). Designer's deliberate calls: NO shared-component extraction (single consumer — avoid speculative abstraction), no entrance animation. tsc+eslint clean, suite still 415/1-known. STAGE 7 SECURITY GATE PASSED (2026-07-14): security-auditor agent line-by-line over the full feature diff = PASS, ZERO exploitable findings (confirmed: no new write path anywhere — booking mutations remain Requests-tab-only; no client-side authz — barberId is the caller's own id and RLS is the sole authority; no information exposure beyond what the individual tabs already show — counterpart names come from the same audited 0012 RPC map; no raw server text can reach UI/prod logs — fetchDashboardView discards failure arms entirely and logging is __DEV__-gated; isUpcomingBooking re-export behavior-preserving/acyclic; all navigation targets static, no params). Two informational notes, no action: INFO-1 fetchDashboardView's never-rejects guarantee rests on sub-reads returning unions not throwing (fragility note for future refactors); INFO-2 spread-Math.min over own services (not a finding). /security-audit PASS: feature diff adds zero dependencies, zero secret-like strings, no env/config change; npm-audit's 11 moderates are the pre-existing Expo BUILD-tooling chain (uuid via xcode/config-plugins), not runtime, unchanged. No /supabase-security-audit re-run needed: zero DB-side change; the same-day portfolio-gate live verification (advisors clean, RLS state) stands. Stage 8 Maestro deliverables DONE: .maestro/barber-dashboard-overview-readiness.yaml authored (deterministic path only: sections render in any backend state, overview deep-links to Requests, silent focus-refresh on return; state-dependent assertions deliberately excluded — see file header; unexecuted, same CLI gap) + .maestro/README.md updated (run list, rationale section, testID registry). STAGE 8 database-optimizer DONE (2026-07-14): verdict ACCEPTABLE AT MVP, no changes to ship. C4 ANSWERED — KEEP the duplicate services fetch: the two fetches are different sets (requests-view servicesById = only booking-referenced services; listOwnServices = ALL own services, which the readiness meter needs — deriving from the map would report 0 services for a barber with no bookings, exactly who the meter serves); the right future dedupe is a query-cache layer (React Query), not hand-threaded maps. Index coverage COMPLETE for every dashboard read path (0001's participant/barber_id indexes + PK lookups; do NOT add a composite bookings index — the OR-shaped RLS predicate defeats sort-avoidance anyway). Live EXPLAIN run same day via MCP as an authenticated barber (set_config jwt.claim.sub emulation): BitmapOr over idx_bookings_customer_id + idx_bookings_barber_id confirmed, NO seq scan, in-memory quicksort, 2.2ms — optimizer's expected plan shape verified in reality. STAGE 10 context-manager integration confirmation DONE (2026-07-15): applied the context-manager intent in the main thread (agent IS spawnable but the check is a verification, done inline). CONFIRMED: (a) no cycle — src/shared/bookingTime.ts is a leaf (imports only ../types); src/barber/dashboardData.ts imports ONLY ../shared + barber siblings, ZERO src/customer/ import (architect C1 holds in the shipped tree); src/customer/bookingsData.ts re-exports isUpcomingBooking behavior-preservingly (BookingsScreen unchanged). (b) NO new backend read — fetchDashboardView composes the 5 existing owner-scoped reads only. (c) all 5 StudioScreen deep-links resolve to registered routes — Services/Availability (BarberNavigator stack) + Requests/Portfolio/Verify (BarberTabs); React Navigation bubbles Tab->parent-Stack (same wiring the prior BarberDashboardScreen used). (d) tsc + eslint clean; full suite 415 pass / 1 fail = ONLY the known VerifyScreen cross-suite-pollution fail (passes 5/5 in isolation, confirmed) — NO dashboard test failing; slots.test.ts date-fragility did NOT bite on 2026-07-15. Feature is fully green on the code/pipeline side. REMAINS: ONLY the founder on-device visual review (human action — Studio tab: bookings-overview glance + readiness meter render + deep-links land on the right tabs). -->
- [ ] Dashboard/Requests before-Phase-2 follow-up (database-optimizer 2026-07-14, semantically lossless for the dashboard): `fetchOwnRequestsView` selects EVERY booking ever (no limit/status filter) and feeds all ids to the counterparts RPC — payload + uuid-array grow monotonically with lifetime bookings. Fix when Phase 2 starts: optional `fetchOwnRequestsView(opts?: { statuses?: BookingStatus[] })` applied as `.in('status', ...)`; dashboardData passes ['pending','accepted'] (all the overview can surface — bonus: matches the 0009 partial index predicate); Requests tab keeps calling with no args. Pair with a `.limit`/archived-state answer for the Requests tab's own full-history growth in Phase 2 planning.
- [ ] Barber bio-edit — FULL PIPELINE COMPLETE (T1-T9, 2026-07-15 → 2026-07-19). Only the founder on-device review remains: open Studio → Bio card → BioEditScreen, save a bio, confirm the readiness meter recomputes to "N of 5" on return, and check the discard prompt appears if you back out mid-edit. Shipped: migration 0018 (bundled bio/messages/portfolio CHECKs, live-verified), `updateOwnBio` (writes only {bio}, RLS sole authz, empty→NULL), BioEditScreen (mount re-fetch per C5, plain useEffect per C6, beforeRemove discard guard), Studio Bio card, bio as the 5th readiness item, Ultra UI pass (13 fixes), 7 component tests, `.maestro/barber-bio-edit.yaml` (unexecuted — repo-wide Maestro CLI gap). Gates: security PASS WITH FINDINGS (findings tracked on their own lines below; adversarial JWT probes confirmed RLS blocks cross-barber writes and the 0005 trigger blocks self-approval), optimizer ACCEPTABLE AT MVP (every path a unique-index single-row lookup; bio added ZERO queries — it rode an existing `select('*')`), context-manager clean (no cycles, all 5 readiness deep-links resolve). SETTLED, DO NOT RE-LITIGATE: bio counts toward `isLive` — the meter means "profile fully set up", NOT literal searchability (the DB gates customer visibility on approved verification alone).

### Findings from the bio-edit T8 security gate (2026-07-19) — own lines, own runs
- [ ] M2 (MEDIUM, latent): no test pins the portfolio object-path format against `chk_portfolio_image_url_folder` (0018). It holds by construction today because `portfolioData.ts` writes `{barberId}/img-...`, but a future rename of that helper silently turns every portfolio insert into a 23514 surfacing as generic `invalid_input`. Add a unit test asserting the generated path's first segment equals the barber id, so the coupling is explicit rather than incidental. (Same class as the bio-edit lesson: mocked tests can't see constraints, so pin the payload shape.)
- [ ] L1 (LOW, user-visible wrong copy): `src/barber/errors.ts:33-35` maps `invalid_input` to "That entry is not valid. Check the values (including day, date and times) and try again." — copy written for availability windows, and nonsense on the bio screen (`BioEditScreen.tsx:135` renders it). Only reachable via a raw over-length caller, so low impact. Fix by adding a specific code or generalising the shared copy with a caller override. Fold into whichever run next touches `errors.ts`.
- [ ] L2 → Phase 2, NOT a defect today: `bio` is public (via `barber_directory`, readable by every authenticated user), free-text and unmoderated. Length is now bounded; content is not. No XSS vector (RN `<Text>` renders as text, `numberOfLines` caps layout damage), but a barber can publish contact details or off-platform booking instructions to all customers. This becomes a real disintermediation/liability issue once Stripe Connect lands — surface it alongside the dispute-policy gap when Phase 2 planning starts.

### Step 18 — MVP release gate
- [ ] Full end-to-end run with real test users: signup → verify barber → add service/availability → discover → book → accept (realtime) → chat → complete → review
- [ ] **REVIEW-ATTRIBUTION FORGERY — a real, live, currently-exploitable hole in `reviews`. Fix it in the reviews run's FIRST migration, before any aggregation ships.** Found by task-decomposition 2026-07-21 and then PROVEN by live rolled-back probe as a real `authenticated` session (`set local role authenticated` + JWT claims, so RLS was genuinely enforced — not a code read). `reviews_insert_own_customer`'s WITH CHECK is `customer_id = auth.uid()` and NOTHING else, and the only trigger (`enforce_review_requires_completed_booking`) checks solely that the booking is `completed` — it never compares the review's `customer_id`/`barber_id` against the booking's actual participants. Measured results: (A) posting a review against your OWN completed booking but attributing it to an **arbitrary, uninvolved barber** = **ALLOWED**; (B) posting one on behalf of another customer = BLOCKED 42501 (so the WITH CHECK does hold for the reviewer's own identity — the decomposition's wider claim was overstated); (C) reviewing a pending booking = BLOCKED P0001 (trigger holds). So the precise exploit is: any authenticated customer can 1-star **any barber in the system** using a completed booking id they legitimately own, and once rating aggregation lands this silently poisons that barber's public rating. Not reachable through the app today (no review UI exists), but fully reachable by any raw-API caller holding the shipped anon key. Same class as the price/attribution vector migration 0010 had to close. Fix = an `EXISTS` predicate tying `booking_id` to the row's `customer_id` AND `barber_id`, plus the missing `has_role('customer')` check (absent here, though present on four other tables since 0004 — a barber setting `customer_id` to their own uid currently passes). `supabase-schema-architect`-owned; verify with the same adversarial probe afterwards.
- [ ] Reviews: customer rates after a completed booking only (REVIEWS constraint), rating aggregation onto barber_profile via a server-owned path (rating column is trigger-protected — needs schema-architect involvement). Stage 1 decomposition DONE 2026-07-21 (22 tasks / 6 tracks / ~35-45h critical path). FOUNDER DECISIONS LOCKED 2026-07-21: **D1** = NO schema change — a booking stays 'completed' forever, "reviewed?" = a `reviews` row exists for the booking_id (UNIQUE), the 'rated'/'archived' tail is informal language only. **D2** = show the reviewer's FIRST NAME plus a "Verified" tag — BUT FLAGGED to the founder: customers have no verification status (verification is barber-only by deliberate earlier decision; the customer verified badge was removed), and every review is tied to a completed booking by the trigger, so a "Verified" tag is TRUE for every review by construction — same no-op-by-construction as the Explore "Verified" chip; recorded as "verified booking/purchase" semantics pending founder reconfirm if they meant name-hidden "Verified customer" instead. The first-name display still needs the narrow server-side projection (get_booking_counterparts-shaped) since 0006 blocks joining users.name. **D3** = IMMUTABLE — no edit/delete after posting; no UPDATE/DELETE policy; aggregation trigger only handles AFTER INSERT. **D5** = NO barber replies in this slice (clean separate run later if wanted). Sensible defaults adopted for the rest (reconfirm if wrong): **D4** comment OPTIONAL (already nullable, zero schema change); **D6** moot for delete given D3-immutable, and a later-unapproved barber keeps their historical rating; **D7** one review per BOOKING (already fixed by booking_id UNIQUE); **D8** barber-side reviews surface = DEFER (customer-facing display only this slice); **D9** aggregate shows on Discover/Explore cards (BarberCard RatingLine already reads barber_profile.rating, so real numbers appear there automatically once aggregation ships). Recommended aggregation mechanism: an `AFTER INSERT` `SECURITY DEFINER` trigger on `reviews` that recomputes `avg(rating)`, using transaction-local `set_config('request.jwt.claim.role','service_role',true)` to get past migration 0005's `protect_barber_verification_fields` revert — NOTE this would be the codebase's first WRITE-side role-impersonation trigger (the two existing SECURITY DEFINER functions are read-only projections), so it needs a real architect-review gate, not a rubber stamp, and its top risk is failing silently and leaving `rating` at 0 forever — exactly the verification-submission failure shape that mocked tests cannot see. NINE open founder decisions must be answered BEFORE the design gate: D1 the documented `completed → rated → archived` tail does not exist in the DB (`booking_status_type` has only pending/accepted/rejected/completed/cancelled) — is that tail aspirational (answer: no schema change, "reviewed?" = does a `reviews` row exist, which `reviews.booking_id UNIQUE` already makes a single lookup) or real?; D2 what identity shows on a public review (since 0006 a reviewer's name can NOT be joined from `users` — 0006's own comment flags this for whoever builds reviews); D3 edit/delete after posting (today NO update/delete policy exists at all, so reviews are permanently immutable via the API); D4 comment required or optional (already nullable — star-only works with zero schema change); D5 can a barber respond (nothing in the schema for it); D6 aggregate behaviour on delete / on a barber later un-approved; D7 confirm one-review-per-BOOKING (already decided by `booking_id UNIQUE` — a repeat customer gets a second independent review); D8 does the barber see their own reviews anywhere; D9 does the aggregate show on Discover/Explore cards (`BarberCard`'s RatingLine already reads `barber_profile.rating`, so it starts showing real numbers automatically once aggregation ships, wanted or not). Confirmed in passing: `reviews` is NOT Realtime-enabled, so `supabase-realtime-optimizer` is explicitly out of scope; `idx_reviews_barber_id`/`idx_reviews_customer_id` already exist, so no new index is expected.
- [ ] Final /security-audit + /supabase-security-audit across the whole app before Phase 2 planning
- [ ] Extract `PrimaryButton` + `ScreenBackHeader` + `Notice` as shared ultra components AT the Step 18 design pass (evidenced by the Ultra designer during the bio-edit T6 pass, 2026-07-17 — this is NOT the speculative-abstraction case the 2026-07-14 dashboard pass declined). Evidence: `backButton` (36×36 circle) duplicated in 8 files, `primaryButton` in 7 across TWO silently-diverged variants (barber `paddingVertical:14`/`fontSize:15`/icon+row vs customer `paddingVertical:16`/`fontSize:16`/no icon), `notice`/`noticeText` in ~6. BioEditScreen and LocationEditScreen are line-for-line twins (the latter cloned from the former) that DRIFTED WITHIN A WEEK: pressed states, `noticeInline`, `selectionColor`/`cursorColor`/`keyboardAppearance`. Deliberately deferred out of the bio run (one feature per run; LocationEditScreen belongs to the other founder's in-flight Explore Run A and touching it invites a conflict for zero bio benefit). Reconcile the two button variants in the same pass. ALSO carry these same-class LocationEditScreen fixes into that pass (identified but deliberately not applied 2026-07-17): the `noticeInline` double-inset fix, the counter/limit a11y treatment, and `selectionColor`/`cursorColor`/`keyboardAppearance`. AND: the stateful-a11y-label gap on the Services/Availability/Location Studio cards (`accessibilityLabel` overrides children, so a screen-reader user never hears the card's state — bio card fixed 2026-07-17, the rest were out of that run's surface). AND consider collapsing the readiness rows when `isLive` (5 inert muted rows occupy ~220px permanently once complete; deferred because collapsing drops the `barber-dashboard-readiness-{key}` testIDs the 2026-07-14 pass mandated in every state — needs a testID answer first).
- [ ] FOUNDER REMINDER (requested 2026-07-13): before release, do a full design pass over EVERY screen and component with the Ultra ui-ux-designer agent — the agent was upgraded 2026-07-13 to the "Ultra UI/UX Designer" (RN/Expo-native, brand-locked, ultra-component discipline) and is now the standard for all design work, but the earlier screens were built before it existed. Bring them all up to the new bar (start from the booking calendar rebuild — CalendarDateStrip + grouped time slots — as the reference standard). Do NOT let this reminder silently disappear.

### Phase 2 planning reminders (do not build yet — surface when Phase 2 starts)
- [ ] Stripe Connect payments; dispute resolution + refund/cancellation-fee policy (undefined, blocks real money)
- [ ] In-home safety features: live location sharing during appointment window, SOS button (highest-liability gap)
- [ ] Revisit email-change flow (users.email is trigger-frozen; requires service_role-mediated sync)

